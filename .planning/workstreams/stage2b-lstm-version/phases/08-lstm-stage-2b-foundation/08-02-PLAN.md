---
phase: 08-lstm-stage-2b-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - botdetector_pipeline.py
  - tests/conftest.py
  - tests/test_calibrate.py
autonomous: true
requirements:
  - LSTM-01
  - LSTM-02
  - LSTM-03

must_haves:
  truths:
    - "The LSTM Stage 2b variant should preserve the existing refined z2-style compatibility contract"
    - "Phase 8 only needs deterministic fixture-backed train/infer proof, not full cascade benchmarking"
    - "The AMR delta refiner stays intact as the baseline path"
  artifacts:
    - path: "botdetector_pipeline.py"
      provides: "Trainable LSTM Stage 2b prototype with z2-compatible output"
      contains: "parallel-variant foundation and helper APIs"
    - path: "tests/conftest.py"
      provides: "Minimal seeded train/infer support for the LSTM Stage 2b path"
      contains: "fixture path for deterministic proof"
    - path: "tests/test_calibrate.py"
      provides: "Contract-level regression coverage for seeded train/infer behavior"
      contains: "proof that the new path can return stable, z2-compatible refinement outputs"
  key_links:
    - from: "botdetector_pipeline.py"
      to: "tests/conftest.py"
      via: "parallel Stage 2b fixture assembly"
      pattern: "TrainedSystem"
    - from: "botdetector_pipeline.py"
      to: "tests/test_calibrate.py"
      via: "deterministic train/infer proof patterns already used in this repo"
      pattern: "minimal_system"
---

<objective>
Build the minimal trainable LSTM Stage 2b prototype that consumes the Phase 8 sequence contract and proves it can emit a stable, refined `z2`-compatible output under fixture-driven train/infer conditions.

Purpose: Once sequence preparation exists, Phase 8 must show the LSTM variant is more than a preprocessing idea. This plan proves the model contract itself: trainable, seeded, and compatible with the existing Stage 2b role.
Output: A fixture-proven LSTM Stage 2b prototype with deterministic `z2`-style refinement behavior.
</objective>

<execution_context>
@.planning/workstreams/stage2b-lstm-version/phases/08-lstm-stage-2b-foundation/08-CONTEXT.md
@.planning/workstreams/stage2b-lstm-version/phases/08-lstm-stage-2b-foundation/08-01-PLAN.md
@botdetector_pipeline.py
@tests/conftest.py
@tests/test_calibrate.py
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Add a trainable LSTM Stage 2b prototype that preserves the z2 refinement contract</name>
  <files>botdetector_pipeline.py</files>
  <read_first>
    - botdetector_pipeline.py after Plan 01
    - 08-CONTEXT.md decisions D-01, D-05, D-06
    - current AMRDeltaRefiner contract in botdetector_pipeline.py
  </read_first>
  <behavior>
    - Mirror the current Stage 2b role as closely as possible: refine Stage 2a logits instead of redefining the entire stage.
    - Keep the LSTM path additive and clearly separable from the AMR baseline.
    - Favor a minimal prototype sufficient for deterministic proof over a broad architecture search.
  </behavior>
  <action>
Add the minimal model-side implementation for the LSTM Stage 2b variant.

Specifically:
1. Introduce an LSTM-based Stage 2b component that can consume the embedded-message sequence contract from Plan 01.
2. Ensure the component can produce a refined `z2`-compatible output or a clearly documented equivalent that collapses to the same downstream contract.
3. Keep the AMR path untouched and available for future comparison.
4. Limit this plan to the foundation/prototype level rather than full train_system()/predict_system() rollout if that would overreach Phase 8.
  </action>
  <verify>
    <automated>python -m pytest tests/test_calibrate.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - The code contains a trainable LSTM Stage 2b prototype
    - The prototype exposes a refined z2-style contract suitable for later integration
    - The AMR delta-refiner baseline remains available and unchanged in semantics
  </acceptance_criteria>
  <done>The codebase has a viable, additive LSTM Stage 2b prototype rather than only a preprocessing helper.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove deterministic fixture-backed train/infer behavior for the LSTM Stage 2b prototype</name>
  <files>tests/conftest.py, tests/test_calibrate.py</files>
  <read_first>
    - tests/conftest.py
    - tests/test_calibrate.py
    - botdetector_pipeline.py after Task 1
  </read_first>
  <behavior>
    - Reuse the repo's existing deterministic synthetic-test style instead of introducing a heavyweight benchmark harness.
    - Validate the model contract, not Phase 9 integration concerns.
  </behavior>
  <action>
Add fixture-backed checks proving the LSTM prototype is usable and deterministic.

Specifically:
1. Extend fixture support so a minimal train/infer path can exercise the LSTM Stage 2b prototype under a fixed seed.
2. Add tests that verify:
   - seeded train/infer stability
   - empty-history accounts do not crash or produce unstable outputs
   - the LSTM path can emit a refined z2-compatible output shape/value contract
3. Keep the proof scoped: this phase does not need a full AMR-vs-LSTM benchmark yet.
  </action>
  <verify>
    <automated>python -m pytest tests/test_calibrate.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Tests prove the LSTM prototype can train/infer under fixture conditions
    - Seeded behavior is explicit and covered
    - The tests enforce the z2-compatible output contract
    - The proof remains narrow enough to fit Phase 8 scope
  </acceptance_criteria>
  <done>Phase 8 has deterministic proof that the LSTM Stage 2b foundation is trainable and contract-compatible.</done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/test_features_stage2.py -x -q` - sequence invariants remain intact
2. `python -m pytest tests/test_calibrate.py -x -q` - fixture-backed train/infer contract is proven
3. The LSTM Stage 2b prototype can emit a stable refined output compatible with later Stage 2 integration
</verification>

<success_criteria>
- Phase 8 produces a real trainable LSTM Stage 2b foundation, not just design notes
- The LSTM path preserves the intended z2-compatible contract
- Requirements `LSTM-01`, `LSTM-02`, and `LSTM-03` have a concrete implementation path
</success_criteria>
