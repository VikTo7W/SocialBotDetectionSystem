---
phase: 10-evaluation-and-baseline-comparison
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - ablation_tables.py
  - .planning/workstreams/stage2b-lstm-version/phases/10-evaluation-and-baseline-comparison/10-VERIFICATION.md
autonomous: true
requirements:
  - LSTM-07
  - LSTM-08

must_haves:
  truths:
    - "Phase 10 should leave behind a compact comparison report plus reusable table output"
    - "The final result must explicitly communicate whether LSTM is better, worse, or merely different relative to AMR"
    - "A neutral recommendation is a valid successful outcome"
  artifacts:
    - path: "ablation_tables.py"
      provides: "Reusable comparison tables for the AMR-vs-LSTM Phase 10 result"
      contains: "compact side-by-side evaluation output compatible with existing reporting patterns"
    - path: ".planning/workstreams/stage2b-lstm-version/phases/10-evaluation-and-baseline-comparison/10-VERIFICATION.md"
      provides: "Phase-close evidence summary"
      contains: "verification results and final recommendation framing"
  key_links:
    - from: "ablation_tables.py"
      to: "main.py"
      via: "shared real S3 comparison outputs and compact table generation"
      pattern: "table"
---

<objective>
Turn the real AMR-vs-LSTM comparison evidence into the compact milestone-facing outputs Phase 10 needs: reusable tables plus an explicit recommendation record.

Purpose: Real evidence alone is not enough if it is trapped in console output or ad hoc notes. This plan packages the comparison into a compact reporting shape the workstream can keep and reuse.
Output: Reusable comparison tables and milestone-close recommendation artifacts that say clearly whether LSTM beats, loses to, or simply differs from AMR.
</objective>

<execution_context>
@.planning/workstreams/stage2b-lstm-version/phases/10-evaluation-and-baseline-comparison/10-CONTEXT.md
@.planning/workstreams/stage2b-lstm-version/phases/10-evaluation-and-baseline-comparison/10-01-PLAN.md
@ablation_tables.py
@main.py
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Add reusable AMR-vs-LSTM comparison tables</name>
  <files>ablation_tables.py</files>
  <read_first>
    - ablation_tables.py
    - main.py after Plan 01
    - 10-CONTEXT.md decisions D-05 through D-08
  </read_first>
  <behavior>
    - Reuse existing table-building patterns wherever practical.
    - Keep the output compact and milestone-oriented rather than expanding into a large benchmark suite.
    - Make routing differences visible alongside metric differences.
  </behavior>
  <action>
Create reusable table output for the Phase 10 comparison.

Specifically:
1. Add or extend table-generation logic so the AMR-vs-LSTM result can be rendered in a reusable compact form.
2. Include both headline metric comparison and routing-behavior comparison in the output.
3. Keep the output compatible with the repo's current report/table style.
  </action>
  <verify>
    <automated>python -m pytest tests/test_evaluate.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - The Phase 10 result can be rendered as reusable comparison tables
    - Tables expose both metric and routing differences
    - The output stays compact and consistent with existing reporting code
  </acceptance_criteria>
  <done>Phase 10 leaves behind reusable comparison tables instead of only transient console output.</done>
</task>

<task type="auto">
  <name>Task 2: Record the final Phase 10 recommendation honestly</name>
  <files>.planning/workstreams/stage2b-lstm-version/phases/10-evaluation-and-baseline-comparison/10-VERIFICATION.md</files>
  <read_first>
    - 10-CONTEXT.md
    - the real comparison artifact produced in Plan 01
    - any table output produced in Task 1
  </read_first>
  <behavior>
    - State the result plainly: better, worse, or different.
    - Do not force an LSTM win if the evidence does not justify it.
  </behavior>
  <action>
Write the final recommendation framing for the milestone.

Specifically:
1. Summarize the real S3 comparison outcome in compact form.
2. Record whether AMR or LSTM is recommended, or whether AMR remains default while LSTM is behaviorally distinct.
3. Anchor the recommendation in explicit evidence from metrics and routing behavior.
  </action>
  <verify>
    <automated>python -m pytest tests/test_evaluate.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - The final recommendation is explicit and evidence-backed
    - Neutral outcomes are represented cleanly
    - Requirement `LSTM-08` has a concrete documentation path
  </acceptance_criteria>
  <done>The workstream records an honest final Phase 10 recommendation grounded in the real comparison evidence.</done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/test_evaluate.py -x -q` - comparison/report contract remains valid
2. The repo can emit compact reusable tables for the AMR-vs-LSTM comparison
3. The milestone artifacts can state an evidence-backed recommendation without forcing a challenger win
</verification>

<success_criteria>
- Phase 10 leaves behind compact tables and a clear recommendation artifact
- The recommendation truthfully states whether LSTM is better, worse, or merely different
- Requirements `LSTM-07` and `LSTM-08` have an executable implementation path
</success_criteria>
