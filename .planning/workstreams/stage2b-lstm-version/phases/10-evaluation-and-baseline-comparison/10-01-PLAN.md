---
phase: 10-evaluation-and-baseline-comparison
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - main.py
  - evaluate.py
  - tests/test_evaluate.py
autonomous: true
requirements:
  - LSTM-07

must_haves:
  truths:
    - "Phase 10 must judge AMR vs LSTM on the real BotSim S3 path, not only on fixture-backed or synthetic evidence"
    - "What counts as better includes both top-line evaluation metrics and routing behavior"
    - "The recommendation must remain neutral enough to keep AMR as the outcome if LSTM is only different, not clearly better"
  artifacts:
    - path: "main.py"
      provides: "Real-path AMR-vs-LSTM comparison execution and persisted comparison artifact"
      contains: "shared split/calibration/evaluation flow for both Stage 2b variants"
    - path: "evaluate.py"
      provides: "Reusable comparison summary helpers"
      contains: "metric-plus-routing comparison surface for AMR vs LSTM"
    - path: "tests/test_evaluate.py"
      provides: "Regression coverage for comparison artifact and recommendation contract"
      contains: "deterministic checks for AMR-vs-LSTM reporting behavior"
  key_links:
    - from: "main.py"
      to: "evaluate.py"
      via: "real S3 variant comparison and recommendation artifact generation"
      pattern: "evaluate_s3"
    - from: "evaluate.py"
      to: "tests/test_evaluate.py"
      via: "artifact/report contract protection"
      pattern: "routing"
---

<objective>
Run the real S3 AMR-vs-LSTM comparison through the existing cascade entrypoint and emit a compact evidence artifact that can support a final recommendation.

Purpose: Phase 9 proved that both Stage 2b variants can run correctly. Phase 10 starts by producing the real comparison evidence the milestone actually needs: same split discipline, same threshold policy, same cascade, two explicit Stage 2b variants.
Output: A real-path AMR-vs-LSTM comparison artifact covering overall metrics, routing behavior, and a preliminary recommendation basis.
</objective>

<execution_context>
@.planning/workstreams/stage2b-lstm-version/phases/10-evaluation-and-baseline-comparison/10-CONTEXT.md
@.planning/workstreams/stage2b-lstm-version/REQUIREMENTS.md
@main.py
@evaluate.py
@tests/test_evaluate.py
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Add real S3 comparison execution for AMR vs LSTM</name>
  <files>main.py, evaluate.py</files>
  <read_first>
    - main.py
    - evaluate.py
    - 10-CONTEXT.md decisions D-01 through D-08
    - current Phase 9 integration summaries
  </read_first>
  <behavior>
    - Keep the comparison apples-to-apples: same split flow, same threshold policy, only Stage 2b variant changes.
    - Reuse existing real-path entrypoints and reporting helpers instead of building a separate benchmark stack.
    - Make the evidence legible enough to justify a later recommendation.
  </behavior>
  <action>
Build the real BotSim S3 comparison path for AMR vs LSTM.

Specifically:
1. Extend the real evaluation flow so it can train or load both `amr` and `lstm` Stage 2b variants under the same pipeline conditions.
2. Capture both overall metrics and routing behavior for the two variants on S3.
3. Emit a compact machine-readable artifact summarizing:
   - variant names
   - key metrics
   - routing deltas
   - enough evidence to support a final recommendation
4. Preserve existing evaluation semantics outside the Stage 2b comparison additions.
  </action>
  <verify>
    <automated>python -m pytest tests/test_evaluate.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - The repo can produce a real S3 AMR-vs-LSTM comparison artifact
    - The artifact includes both metric and routing evidence
    - The comparison keeps the rest of the cascade path shared and controlled
  </acceptance_criteria>
  <done>The codebase can generate the real evidence Phase 10 needs for a neutral AMR-vs-LSTM judgment.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Protect the AMR-vs-LSTM comparison contract with deterministic tests</name>
  <files>tests/test_evaluate.py</files>
  <read_first>
    - tests/test_evaluate.py
    - evaluate.py after Task 1
    - 10-CONTEXT.md decisions D-03 through D-08
  </read_first>
  <behavior>
    - Prefer compact contract tests over brittle golden-output snapshots.
    - Protect the structure of the comparison artifact and recommendation-ready evidence.
  </behavior>
  <action>
Add test coverage for the new Phase 10 comparison surface.

Specifically:
1. Add tests for the AMR-vs-LSTM comparison summary/artifact structure.
2. Verify the comparison output includes both metric and routing evidence.
3. Ensure the comparison logic can represent neutral outcomes rather than forcing a winner.
  </action>
  <verify>
    <automated>python -m pytest tests/test_evaluate.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Comparison helpers are covered by deterministic tests
    - Tests enforce metric-plus-routing evidence
    - Tests allow a neutral recommendation outcome
  </acceptance_criteria>
  <done>Phase 10 has automated protection around its real comparison evidence contract.</done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/test_evaluate.py -x -q` - comparison/report helpers pass deterministic coverage
2. The real evaluation path can compare both Stage 2b variants under shared conditions
3. The output artifact contains enough evidence to support a neutral or decisive recommendation
</verification>

<success_criteria>
- Phase 10 has a real S3 AMR-vs-LSTM comparison path
- The comparison includes both quality metrics and routing behavior
- Requirement `LSTM-07` has a concrete execution path
</success_criteria>
