---
phase: 13-system-version-packaging-and-release-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - VERSION.md
autonomous: true
requirements:
  - VERS-01
must_haves:
  truths:
    - "A developer can open a single VERSION.md file at the project root and learn which model artifact, evaluation modes, and output files define the v1.3 TwiBot system version"
    - "The VERSION.md file names trained_system_v12.joblib as the active model artifact"
    - "The VERSION.md file lists both evaluation modes (static and recalibrated) and points at the Phase 12 artifacts directory as the canonical output location"
  artifacts:
    - path: "VERSION.md"
      provides: "explicit identification of the v1.3 TwiBot system version"
      min_lines: 60
      contains: "trained_system_v12.joblib"
  key_links:
    - from: "VERSION.md"
      to: ".planning/workstreams/milestone/phases/12-fresh-transfer-evidence-and-paper-outputs/artifacts/"
      via: "documented artifact output path"
      pattern: "12-fresh-transfer-evidence-and-paper-outputs/artifacts"
    - from: "VERSION.md"
      to: "evaluate_twibot20.py"
      via: "named canonical evaluation entry point"
      pattern: "evaluate_twibot20.py"
    - from: "VERSION.md"
      to: "ablation_tables.py"
      via: "named paper-output entry point"
      pattern: "ablation_tables.py"
---

<objective>
Create a root-level `VERSION.md` that explicitly identifies the v1.3 TwiBot system version: which model artifact ships, which evaluation modes are supported, and where the canonical output artifacts live. This is pure documentation — no code changes, no model runs, no new experiments. It packages what Phases 8-12 already shipped.

Purpose: satisfy `VERS-01` by giving a future developer (or reviewer) a single authoritative file naming the release contract.

Output:
- `VERSION.md` at the project root, cross-referenced from subsequent docs work in Plan 13-02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/workstreams/milestone/ROADMAP.md
@.planning/workstreams/milestone/STATE.md
@.planning/workstreams/milestone/phases/12-fresh-transfer-evidence-and-paper-outputs/12-01-SUMMARY.md
@.planning/workstreams/milestone/phases/12-fresh-transfer-evidence-and-paper-outputs/12-02-SUMMARY.md
@.planning/workstreams/milestone/phases/12-fresh-transfer-evidence-and-paper-outputs/artifacts/transfer_evidence_summary.json
@evaluate_twibot20.py
@ablation_tables.py

<interfaces>
<!-- Facts extracted from live code and artifacts. Use these values verbatim. -->

Active model artifact: `trained_system_v12.joblib`

Canonical evaluation command (from evaluate_twibot20.py docstring, Phase 11):
    python evaluate_twibot20.py <test_json> <model_joblib> [output_dir]

Default output_dir (Phase 12, from evaluate_twibot20.py PHASE12_EVIDENCE_DIR):
    .planning/workstreams/milestone/phases/12-fresh-transfer-evidence-and-paper-outputs/artifacts

Expected output files (evaluate_twibot20.EXPECTED_OUTPUT_FILES + downstream paper path):
    - results_twibot20.json
    - metrics_twibot20.json
    - metrics_twibot20_comparison.json
    - transfer_evidence_summary.json
    - tables/table5_cross_dataset.tex                (written by ablation_tables.py)
    - tables/table5_transfer_interpretation.txt     (written by ablation_tables.py)

Evaluation modes (from evaluate_twibot20.py compare_twibot20_conditions):
    - static       — online_calibration=False, trained cascade thresholds only
    - recalibrated — online_calibration=True,  Phase 9 sliding-window novelty-threshold updates

Live Phase 12 verdict (from transfer_evidence_summary.json):
    - static_overall:       f1=0.0, auc=0.5964
    - recalibrated_overall: f1=0.0, auc=0.5879
    - delta_overall f1:     0.0    (no_material_change, interpretation_basis=f1_delta)

Env-var overrides available (Phase 11):
    - TWIBOT_COMPARISON_PATH      — overrides where Table 5 reads comparison metrics from
    - TABLE5_OUTPUT_PATH          — overrides where table5_cross_dataset.tex is written
    - TABLE5_INTERPRETATION_PATH  — overrides where table5_transfer_interpretation.txt is written
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author VERSION.md naming the v1.3 TwiBot system version</name>
  <files>VERSION.md</files>
  <read_first>
    - README.md (current 1-line placeholder — confirm we are not duplicating existing content)
    - evaluate_twibot20.py (lines 1-100 for docstring, plus PHASE12_EVIDENCE_DIR and EXPECTED_OUTPUT_FILES constants)
    - ablation_tables.py (first 40 lines for Table 5 provenance)
    - .planning/workstreams/milestone/phases/12-fresh-transfer-evidence-and-paper-outputs/artifacts/transfer_evidence_summary.json (live verdict numbers)
    - .planning/REQUIREMENTS.md (VERS-01 text)
    - .planning/workstreams/milestone/ROADMAP.md (Phase 13 success criteria)
  </read_first>
  <action>
    Create `VERSION.md` at the project root. Do NOT touch README.md in this plan — Plan 13-02 owns README. The file MUST contain exactly these top-level sections, in this order, with these exact H2 headers:

    ## System Version
    State the release line verbatim: "v1.3 — TwiBot System Version (zero-shot transfer)". State that it is packaged on 2026-04-18. State that it is the zero-shot transfer system version derived from the v1.2 trained cascade — no retraining.

    ## Model Artifact
    Name exactly one active artifact: `trained_system_v12.joblib` (include the filename as inline code). State that it is the v1.2 clean, leakage-audited cascade produced in Phase 5 and reused unchanged for TwiBot transfer. State that `trained_system_v11.joblib` is preserved for ablation purposes but is NOT the released system version.

    ## Evaluation Entry Points
    Name `evaluate_twibot20.py` as the evaluation entry point and `ablation_tables.py` as the paper-output entry point. Show the canonical command as a fenced bash block exactly as documented in the evaluate_twibot20.py docstring:
    ```bash
    python evaluate_twibot20.py <test_json> <model_joblib> [output_dir]
    ```
    Show the concrete invocation actually used in Phase 12 as a second fenced bash block:
    ```bash
    python evaluate_twibot20.py test.json trained_system_v12.joblib \
      .planning/workstreams/milestone/phases/12-fresh-transfer-evidence-and-paper-outputs/artifacts
    python ablation_tables.py
    ```

    ## Evaluation Modes
    List exactly two modes as a bullet list, both produced by a single invocation of the canonical command:
    - `static` — online_calibration=False; uses the trained cascade thresholds unchanged.
    - `recalibrated` — online_calibration=True; applies the Phase 9 sliding-window novelty-threshold recalibration (default window_size=100).

    ## Expected Output Files
    List exactly these filenames as a bullet list with one-line descriptions. Use the Phase 12 artifact directory as the default output path.

    Written to `.planning/workstreams/milestone/phases/12-fresh-transfer-evidence-and-paper-outputs/artifacts/`:
    - `results_twibot20.json` — per-account cascade outputs (11 keys per record).
    - `metrics_twibot20.json` — overall/per-stage/routing metrics for the recalibrated run.
    - `metrics_twibot20_comparison.json` — static vs recalibrated comparison plus deltas (consumed by Table 5).
    - `transfer_evidence_summary.json` — compact Phase 12 evidence summary with interpretation verdict.

    Written to `tables/`:
    - `table5_cross_dataset.tex` — paper-ready cross-dataset LaTeX table.
    - `table5_transfer_interpretation.txt` — human-readable transfer-result interpretation.

    ## Release-Time Transfer Verdict
    Quote the live Phase 12 numbers verbatim from transfer_evidence_summary.json and state the verdict: static F1=0.0, AUC=0.5964; recalibrated F1=0.0, AUC=0.5879; delta F1=0.0; verdict = `no_material_change` (interpretation_basis = `f1_delta`).

    ## Environment Overrides
    List as a bullet list all three env-vars currently honored by the v1.3 output paths:
    - `TWIBOT_COMPARISON_PATH` — override the comparison metrics file that Table 5 reads.
    - `TABLE5_OUTPUT_PATH` — override where `table5_cross_dataset.tex` is written.
    - `TABLE5_INTERPRETATION_PATH` — override where `table5_transfer_interpretation.txt` is written.

    ## Cross-References
    End with a one-line pointer: "See `README.md` for end-to-end reproduction steps and known caveats." (Plan 13-02 will expand README accordingly.)

    Every named file path MUST be copied literally from the source artifacts — no paraphrasing. No new claims, no speculation, no forward-looking language ("will", "future", "planned"). Document only what is already shipped.
  </action>
  <verify>
    <automated>test -f VERSION.md && grep -q "trained_system_v12.joblib" VERSION.md && grep -q "evaluate_twibot20.py" VERSION.md && grep -q "ablation_tables.py" VERSION.md && grep -q "12-fresh-transfer-evidence-and-paper-outputs/artifacts" VERSION.md && grep -q "results_twibot20.json" VERSION.md && grep -q "metrics_twibot20.json" VERSION.md && grep -q "metrics_twibot20_comparison.json" VERSION.md && grep -q "transfer_evidence_summary.json" VERSION.md && grep -q "table5_cross_dataset.tex" VERSION.md && grep -q "table5_transfer_interpretation.txt" VERSION.md && grep -q "no_material_change" VERSION.md && grep -q "TWIBOT_COMPARISON_PATH" VERSION.md && grep -q "TABLE5_OUTPUT_PATH" VERSION.md && grep -q "TABLE5_INTERPRETATION_PATH" VERSION.md</automated>
  </verify>
  <acceptance_criteria>
    - VERSION.md exists at the project root.
    - VERSION.md contains, each as a literal substring: `trained_system_v12.joblib`, `evaluate_twibot20.py`, `ablation_tables.py`, `results_twibot20.json`, `metrics_twibot20.json`, `metrics_twibot20_comparison.json`, `transfer_evidence_summary.json`, `table5_cross_dataset.tex`, `table5_transfer_interpretation.txt`, `12-fresh-transfer-evidence-and-paper-outputs/artifacts`, `no_material_change`, `TWIBOT_COMPARISON_PATH`, `TABLE5_OUTPUT_PATH`, `TABLE5_INTERPRETATION_PATH`.
    - VERSION.md contains these exact H2 headers (`##`): `System Version`, `Model Artifact`, `Evaluation Entry Points`, `Evaluation Modes`, `Expected Output Files`, `Release-Time Transfer Verdict`, `Environment Overrides`, `Cross-References`.
    - VERSION.md lists both `static` and `recalibrated` as distinct evaluation modes.
    - VERSION.md states the verdict numbers exactly as in transfer_evidence_summary.json: static F1=0.0, static AUC=0.5964, recalibrated F1=0.0, recalibrated AUC=0.5879.
    - VERSION.md references `v1.3` explicitly.
    - No forward-looking language (no "will", "future", "planned", "TBD", "TODO") appears in VERSION.md.
  </acceptance_criteria>
  <done>
    VERSION.md committed at project root, grep verification passes, and VERS-01 is visibly satisfied by the file content.
  </done>
</task>

</tasks>

<verification>
- `test -f VERSION.md` passes.
- The automated grep chain in Task 1's `<verify>` block returns exit 0.
- Manual read of VERSION.md confirms every required header and substring is present and no forward-looking language appears.
- VERS-01 traceability row in REQUIREMENTS.md can now be marked satisfied by Plan 13-01.
</verification>

<success_criteria>
1. `VERSION.md` exists at the project root and explicitly names `trained_system_v12.joblib` as the active model artifact.
2. Both `static` and `recalibrated` evaluation modes are named.
3. All six expected output filenames plus the two `tables/` artifacts are listed.
4. The live Phase 12 verdict (static F1=0.0, AUC=0.5964; recalibrated F1=0.0, AUC=0.5879; `no_material_change`) is recorded at release time.
5. All three environment overrides (`TWIBOT_COMPARISON_PATH`, `TABLE5_OUTPUT_PATH`, `TABLE5_INTERPRETATION_PATH`) are listed.
</success_criteria>

<output>
After completion, create `.planning/workstreams/milestone/phases/13-system-version-packaging-and-release-docs/13-01-SUMMARY.md`
</output>
