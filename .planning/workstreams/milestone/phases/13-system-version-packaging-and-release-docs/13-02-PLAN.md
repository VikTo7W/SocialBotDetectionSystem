---
phase: 13-system-version-packaging-and-release-docs
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - README.md
autonomous: true
requirements:
  - VERS-02
  - VERS-03
must_haves:
  truths:
    - "A developer reading README.md can follow a numbered sequence of commands and regenerate the v1.3 TwiBot system-version artifacts end to end"
    - "A developer reading README.md knows, before running, the remaining execution caveats (pytest tmp_path friction on Windows), environment assumptions (Python version, seed, input file locations), and known limitations (TwiBot zero-shot F1=0.0, AMR embedding stub)"
    - "README.md cross-references VERSION.md so the release contract and the reproduction steps stay linked"
  artifacts:
    - path: "README.md"
      provides: "end-to-end reproduction guide plus caveats and limitations section for the v1.3 TwiBot system version"
      min_lines: 80
      contains: "evaluate_twibot20.py"
  key_links:
    - from: "README.md"
      to: "VERSION.md"
      via: "explicit link/reference to the release contract"
      pattern: "VERSION.md"
    - from: "README.md"
      to: "evaluate_twibot20.py"
      via: "canonical command in Reproduction section"
      pattern: "python evaluate_twibot20.py"
    - from: "README.md"
      to: "ablation_tables.py"
      via: "paper output step in Reproduction section"
      pattern: "python ablation_tables.py"
    - from: "README.md"
      to: ".planning/workstreams/milestone/phases/12-fresh-transfer-evidence-and-paper-outputs/artifacts/"
      via: "expected output directory in Reproduction section"
      pattern: "12-fresh-transfer-evidence-and-paper-outputs/artifacts"
---

<objective>
Expand the currently near-empty `README.md` with two release-facing sections: an end-to-end reproduction guide for the v1.3 TwiBot system-version artifacts (VERS-02), and a caveats/assumptions/limitations section (VERS-03). Both sections document the existing Phase 8-12 implementation — no new code, no new experiments.

Purpose: satisfy `VERS-02` and `VERS-03` so a future developer can reproduce the live Phase 12 artifacts and knows the known caveats before running.

Output:
- updated `README.md` with a Reproduction Guide section and a Caveats & Limitations section, cross-linked to `VERSION.md` from Plan 13-01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/workstreams/milestone/ROADMAP.md
@.planning/workstreams/milestone/STATE.md
@.planning/workstreams/milestone/phases/12-fresh-transfer-evidence-and-paper-outputs/12-01-SUMMARY.md
@.planning/workstreams/milestone/phases/12-fresh-transfer-evidence-and-paper-outputs/12-02-SUMMARY.md
@.planning/workstreams/milestone/phases/12-fresh-transfer-evidence-and-paper-outputs/artifacts/transfer_evidence_summary.json
@VERSION.md
@evaluate_twibot20.py
@ablation_tables.py

<interfaces>
<!-- Facts extracted from live sources. Use verbatim. -->

Current README.md content (pre-plan): a single line "# SocialBotDetectionSystem". All other README content must be authored in this plan.

Canonical reproduction command chain (Phase 11-12, from evaluate_twibot20.py docstring and 12-01-SUMMARY.md):
    python evaluate_twibot20.py test.json trained_system_v12.joblib \
      .planning/workstreams/milestone/phases/12-fresh-transfer-evidence-and-paper-outputs/artifacts
    python ablation_tables.py

Default inputs:
    - test.json                  — TwiBot-20 test split (gitignored per .gitignore)
    - trained_system_v12.joblib  — active v1.2 model (gitignored per .gitignore)

Project constraints (from PROJECT.md):
    - Tech stack: Python, scikit-learn, LightGBM, sentence-transformers, PyTorch (tensor loading)
    - SEED=42 across experiments
    - No retraining on TwiBot-20 (zero-shot only)

Known caveats (from STATE.md "Blockers/Concerns" and PROJECT.md "Current State"):
    - Windows temp/process permission issues cause pytest tmp_path cleanup warnings
      (pytest-level only; production code uses zero tempfile/gettempdir — confirmed Phase 11)
    - The .pytest_cache directory is permission-constrained in the current environment
    - Fresh Phase 12 run: static F1=0.0 / AUC=0.5964, recalibrated F1=0.0 / AUC=0.5879
      → recalibration changes Stage 3 routing on TwiBot but does NOT improve final F1
    - Stage 2b AMR refinement is still an embedding-based proxy, not true AMR graph parsing
    - TwiBot F1=0.0 is the weak zero-shot transfer point; it is expected given no retraining

Inputs gitignored (from .gitignore):
    - dev.json, test.json, train.json, trained_system_v11.joblib, trained_system_v12.joblib

Out-of-scope items (from REQUIREMENTS.md):
    - Retraining the main cascade on TwiBot-20
    - New social platform integrations beyond TwiBot-20
    - Frontend/dashboard work
    - Full architectural rewrite of the cascade
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand README.md with the v1.3 Reproduction Guide (VERS-02)</name>
  <files>README.md</files>
  <read_first>
    - README.md (current 1-line content — confirm baseline before overwriting)
    - VERSION.md (produced by Plan 13-01 — README must link to it)
    - evaluate_twibot20.py (module docstring and PHASE12_EVIDENCE_DIR for exact paths)
    - ablation_tables.py (first 40 lines for Table 5 output paths)
    - .planning/workstreams/milestone/phases/12-fresh-transfer-evidence-and-paper-outputs/12-01-SUMMARY.md (the real command that produced the shipped artifacts)
    - .planning/PROJECT.md (tech stack and SEED=42 note)
    - .gitignore (to confirm which inputs are user-supplied and not in the repo)
  </read_first>
  <action>
    Replace the current single-line `README.md` entirely. Preserve the top heading but author full release-facing content. The new README MUST contain exactly these top-level sections, in this order, with these exact H2 headers (additional prose below each header is allowed):

    # SocialBotDetectionSystem

    ## Overview
    One short paragraph (3-5 sentences): the project is a three-stage cascade (metadata → content/temporal → graph) for bot detection, trained on BotSim-24, and v1.3 ships the zero-shot transfer path to TwiBot-20. Point to `VERSION.md` for the full release contract — use the exact phrase "See `VERSION.md` for the v1.3 release contract" somewhere in this section.

    ## Environment Assumptions
    A bullet list stating:
    - Python (match whatever is already in use — the codebase runs on CPython 3.13 per existing `.pyc` paths; state "Python 3.13" explicitly).
    - Dependencies: `scikit-learn`, `lightgbm`, `sentence-transformers`, `torch` (tensor loading only), `joblib`, `numpy`, `pandas`. State that dependencies are installed manually (no pinned requirements file currently ships).
    - Deterministic seed: `SEED=42`.
    - Operating system note: the release was packaged on Windows 10 but the production code path contains no `tempfile`/`gettempdir` usage (pytest-level friction only — see Caveats).

    ## Required Inputs (User-Supplied)
    A bullet list of the gitignored inputs the developer must provide locally before running:
    - `test.json` — TwiBot-20 test split (gitignored; developer must obtain from the TwiBot-20 dataset release).
    - `trained_system_v12.joblib` — v1.2 trained cascade (gitignored; produced by the v1.2 training pipeline in Phase 5 of this repo).

    State clearly that `train.json`, `dev.json`, and `trained_system_v11.joblib` are gitignored too but are NOT required for the v1.3 system-version reproduction (v1.3 is zero-shot inference only).

    ## Reproduction Guide
    A numbered list of at least four steps. Each step MUST include a fenced `bash` code block with the exact command. Do NOT paraphrase commands.

    1. Confirm required inputs (`test.json`, `trained_system_v12.joblib`) sit at the repo root.
    2. Run the canonical TwiBot evaluation. Use this exact fenced block:
       ```bash
       python evaluate_twibot20.py test.json trained_system_v12.joblib \
         .planning/workstreams/milestone/phases/12-fresh-transfer-evidence-and-paper-outputs/artifacts
       ```
       State that this writes four files to the `.planning/.../artifacts` directory: `results_twibot20.json`, `metrics_twibot20.json`, `metrics_twibot20_comparison.json`, `transfer_evidence_summary.json`.
    3. Regenerate Table 5 and the transfer interpretation text. Use this exact fenced block:
       ```bash
       python ablation_tables.py
       ```
       State that this writes `tables/table5_cross_dataset.tex` and `tables/table5_transfer_interpretation.txt`.
    4. (Optional) Redirect Table 5 reads/writes via environment variables. Show a fenced block using all three overrides in a single example, e.g.:
       ```bash
       TWIBOT_COMPARISON_PATH=/alt/path/metrics_twibot20_comparison.json \
       TABLE5_OUTPUT_PATH=/alt/path/table5.tex \
       TABLE5_INTERPRETATION_PATH=/alt/path/table5_interpretation.txt \
       python ablation_tables.py
       ```

    ## Expected Outputs
    Bullet list the exact filenames produced by the canonical run, exactly as enumerated in `VERSION.md`. Include all six: `results_twibot20.json`, `metrics_twibot20.json`, `metrics_twibot20_comparison.json`, `transfer_evidence_summary.json`, `tables/table5_cross_dataset.tex`, `tables/table5_transfer_interpretation.txt`.

    State the live release-time verdict (static F1=0.0 / AUC=0.5964; recalibrated F1=0.0 / AUC=0.5879; `no_material_change`) as a short quoted block, matching VERSION.md.

    Do NOT insert Caveats, Limitations, Out-of-Scope, or License sections in this task — those are reserved for Task 2. But leave the H1 heading and sections above intact so Task 2 only appends.

    Do NOT use forward-looking language ("will", "future", "planned", "TODO", "TBD") in this task.
  </action>
  <verify>
    <automated>test -f README.md && grep -q "VERSION.md" README.md && grep -q "python evaluate_twibot20.py test.json trained_system_v12.joblib" README.md && grep -q "python ablation_tables.py" README.md && grep -q "12-fresh-transfer-evidence-and-paper-outputs/artifacts" README.md && grep -q "results_twibot20.json" README.md && grep -q "metrics_twibot20_comparison.json" README.md && grep -q "transfer_evidence_summary.json" README.md && grep -q "table5_cross_dataset.tex" README.md && grep -q "no_material_change" README.md && grep -q "SEED=42" README.md && grep -q "TWIBOT_COMPARISON_PATH" README.md && grep -q "## Reproduction Guide" README.md && grep -q "## Environment Assumptions" README.md && grep -q "## Required Inputs" README.md && grep -q "## Expected Outputs" README.md</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains the H2 headers `## Overview`, `## Environment Assumptions`, `## Required Inputs (User-Supplied)`, `## Reproduction Guide`, `## Expected Outputs` (all substrings grep-checkable).
    - README.md contains the literal command `python evaluate_twibot20.py test.json trained_system_v12.joblib` followed by the `.planning/workstreams/milestone/phases/12-fresh-transfer-evidence-and-paper-outputs/artifacts` path.
    - README.md contains the literal command `python ablation_tables.py`.
    - README.md references `VERSION.md` at least once.
    - README.md lists all six expected output filenames as literal substrings.
    - README.md contains the release-time verdict string `no_material_change`.
    - README.md states `SEED=42`.
    - README.md mentions all three env-var overrides (`TWIBOT_COMPARISON_PATH`, `TABLE5_OUTPUT_PATH`, `TABLE5_INTERPRETATION_PATH`).
    - README.md contains no forward-looking language strings (no "TODO", no "TBD", no "future", no "will ").
  </acceptance_criteria>
  <done>
    README.md contains the full Reproduction Guide and expected outputs, grep chain passes, and a developer can follow the numbered steps to regenerate the Phase 12 live artifacts without reading any other documentation.
  </done>
</task>

<task type="auto">
  <name>Task 2: Append Caveats and Limitations section to README.md (VERS-03)</name>
  <files>README.md</files>
  <read_first>
    - README.md (post-Task-1 state — do NOT overwrite the Reproduction Guide)
    - .planning/workstreams/milestone/STATE.md (Blockers/Concerns section — authoritative caveat list)
    - .planning/PROJECT.md ("Current State" paragraph about gaps; "Out of Scope" table)
    - .planning/REQUIREMENTS.md ("Out of Scope" table; "Future Requirements" section)
    - .planning/workstreams/milestone/phases/12-fresh-transfer-evidence-and-paper-outputs/artifacts/transfer_evidence_summary.json (live numbers for the zero-shot caveat)
  </read_first>
  <action>
    Append (do NOT rewrite existing content) three new H2 sections at the end of `README.md`, in this order, with these exact headers:

    ## Known Caveats
    Bullet list. Each bullet MUST be specific and sourced from real project state — no invented items. Include at least these items:
    - Windows pytest tmp_path cleanup: `pytest` emits permission warnings on Windows when clearing `.pytest_cache` and `tmp_path`. The production code paths themselves use zero `tempfile`/`gettempdir` calls (confirmed in Phase 11), so this does not affect the canonical evaluation command. State this explicitly.
    - TwiBot zero-shot weakness: at release time the TwiBot transfer produces static F1=0.0 (AUC=0.5964) and recalibrated F1=0.0 (AUC=0.5879); recalibration shifts Stage 3 routing but does NOT improve final F1. Verdict: `no_material_change`. State this explicitly.
    - Stage 2b AMR stub: the v1.3 system version still uses the embedding-based AMR proxy from v1.0, not true AMR graph parsing. State this explicitly.
    - Weak-label calibration artifact: because TwiBot zero-shot predictions are near the decision threshold, precision and recall collapse to 0.0 on the fixed 0.5 threshold. This is a transfer-regime artifact, not a pipeline bug.

    ## Environment Assumptions (Addendum)
    Bullet list (optional — fold into Task 1 section if clearer, but authored here for separation):
    - The release was exercised on Windows 10 with Python 3.13. Linux/macOS have not been re-verified for this specific v1.3 release cut.
    - The gitignored input files (`test.json`, `trained_system_v12.joblib`) must be present at the repo root for the canonical command to succeed.
    - `ablation_tables.py` expects additional BotSim-24 assets (`Users.csv`, `user_post_comment.json`, `edge_index.pt`, `edge_type.pt`, `edge_weight.pt`) at the repo root for the full table set; Table 5 itself only requires the Phase 12 comparison artifact.

    ## Known Limitations (Out of Scope for v1.3)
    Bullet list, each bullet lifted from the Out-of-Scope tables in `REQUIREMENTS.md` and `PROJECT.md`:
    - No TwiBot-20 retraining or supervised adaptation in this release (zero-shot transfer only).
    - No new social platform integrations beyond TwiBot-20.
    - No frontend/dashboard work.
    - No full Twitter-native Stage 2 or Stage 3 redesign (deferred).
    - No multi-seed confidence intervals on cross-domain metrics (deferred).
    - No true AMR graph parsing replacing the current embedding stub (deferred).

    End the README with a single-line cross-reference: "See `VERSION.md` for the exact artifact contract of this release."

    Do NOT introduce forward-looking language ("will fix", "planned", "future work" headers). Plain "deferred" and "out of scope" are acceptable because they describe the current release boundary — not a commitment.
  </action>
  <verify>
    <automated>test -f README.md && grep -q "## Known Caveats" README.md && grep -q "## Known Limitations" README.md && grep -q "tmp_path" README.md && grep -q "F1=0.0" README.md && grep -q "AMR" README.md && grep -q "zero-shot" README.md && grep -q "VERSION.md" README.md && grep -q "no_material_change" README.md</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains H2 headers `## Known Caveats` and `## Known Limitations (Out of Scope for v1.3)` as literal substrings.
    - README.md mentions `tmp_path`, `F1=0.0`, `AMR`, and `zero-shot` as literal substrings.
    - README.md contains the closing cross-reference to `VERSION.md`.
    - All content from Task 1 is still present (the Reproduction Guide, Environment Assumptions, Required Inputs, Expected Outputs sections are intact).
    - No new forward-looking language strings introduced by this task ("TODO", "TBD", "will fix", "planned" must not appear in the sections added by this task).
  </acceptance_criteria>
  <done>
    README.md now fully satisfies VERS-02 (end-to-end reproduction guide) and VERS-03 (caveats, assumptions, limitations). A developer reading README.md alone knows what to run, what to expect, and what NOT to expect.
  </done>
</task>

</tasks>

<verification>
- `test -f README.md && test -f VERSION.md` both pass.
- Both automated grep chains in Task 1 and Task 2 return exit 0.
- Manual read of README.md confirms: Overview → Environment Assumptions → Required Inputs → Reproduction Guide → Expected Outputs → Known Caveats → Known Limitations → VERSION.md cross-reference.
- VERS-02 traceability row in REQUIREMENTS.md can be marked satisfied by Plan 13-02.
- VERS-03 traceability row in REQUIREMENTS.md can be marked satisfied by Plan 13-02.
</verification>

<success_criteria>
1. README.md contains a numbered, copy-pasteable Reproduction Guide with the exact Phase 12 canonical command.
2. README.md lists all six expected output files and the release-time verdict (`no_material_change`).
3. README.md documents the Windows pytest tmp_path caveat, the TwiBot zero-shot F1=0.0 caveat, and the AMR embedding-stub caveat — each explicitly, not as vague hand-waving.
4. README.md cross-references `VERSION.md`.
5. Environment assumptions (Python 3.13, SEED=42, required inputs, additional BotSim-24 assets for `ablation_tables.py`) are stated.
</success_criteria>

<output>
After completion, create `.planning/workstreams/milestone/phases/13-system-version-packaging-and-release-docs/13-02-SUMMARY.md`
</output>
