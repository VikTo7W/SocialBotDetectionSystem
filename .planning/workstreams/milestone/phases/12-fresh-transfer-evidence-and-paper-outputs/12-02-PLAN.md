---
phase: 12-fresh-transfer-evidence-and-paper-outputs
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - ablation_tables.py
  - tests/test_ablation_tables.py
autonomous: true
requirements:
  - EVID-02
  - EVID-03
must_haves:
  truths:
    - "Table 5 can be regenerated from the fresh Phase 12 comparison artifact rather than stale placeholder outputs"
    - "The regenerated paper output reflects the live static and recalibrated TwiBot metrics"
    - "A concise milestone-facing interpretation of the fresh transfer result is written alongside the table output"
  artifacts:
    - path: "ablation_tables.py"
      provides: "fresh-artifact driven Table 5 regeneration path"
      contains: "table5_cross_dataset"
    - path: "tests/test_ablation_tables.py"
      provides: "tests for fresh comparison artifact consumption"
      contains: "test_"
---

<objective>
Consume the fresh Phase 12 comparison artifact to regenerate the final cross-dataset paper output and record whether recalibration materially helped, hurt, or made no meaningful difference.

Purpose: satisfy `EVID-02` and `EVID-03` using live artifacts rather than stale project-root outputs.

Output:
- regenerated `tables/table5_cross_dataset.tex`
- a milestone-facing written interpretation of the fresh transfer result
</objective>

<tasks>

<task>
  <name>Task 1: Regenerate Table 5 from the fresh Phase 12 comparison artifact</name>
  <files>ablation_tables.py</files>
  <action>
    Ensure the Table 5 generation path is cleanly pointed at the fresh Phase 12 comparison artifact and that the resulting LaTeX table reflects those live metrics. Preserve backward-compatible behavior while making the Phase 12 fresh-artifact path explicit in the workflow.
  </action>
  <verify>
    Add or update tests proving the fresh comparison artifact path is consumed correctly.
  </verify>
</task>

<task>
  <name>Task 2: Write the transfer-result interpretation artifact</name>
  <files>ablation_tables.py</files>
  <action>
    Add a small reporting/output path that records the observed transfer outcome from the fresh artifact set in human-readable form. This should state whether recalibration improved, worsened, or did not materially change transfer performance, based on the fresh comparison metrics captured in Plan 12-01.
  </action>
  <verify>
    Confirm the interpretation is derived from the live comparison artifact rather than duplicated hardcoded numbers.
  </verify>
</task>

</tasks>

<verification>
- `python -m py_compile ablation_tables.py tests/test_ablation_tables.py`
- targeted tests for fresh comparison-artifact consumption
- if environment allows, a real `python ablation_tables.py` run with `TWIBOT_COMPARISON_PATH` pointed at the Phase 12 artifact
</verification>

<success_criteria>
1. Table 5 is regenerated from live Phase 12 artifacts.
2. The regenerated paper output uses fresh static and recalibrated TwiBot metrics.
3. The milestone records whether recalibration materially improved transfer quality.
</success_criteria>
