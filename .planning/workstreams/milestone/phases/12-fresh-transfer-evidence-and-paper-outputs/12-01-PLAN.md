---
phase: 12-fresh-transfer-evidence-and-paper-outputs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - evaluate_twibot20.py
  - tests/test_evaluate_twibot20.py
autonomous: true
requirements:
  - EVID-01
must_haves:
  truths:
    - "A fresh TwiBot run can be executed against test.json and trained_system_v12.joblib using the Phase 11 output_dir path"
    - "The run produces results_twibot20.json, metrics_twibot20.json, and metrics_twibot20_comparison.json in a milestone-owned evidence directory"
    - "A stable evidence summary artifact is written from the live comparison JSON"
    - "The evidence summary records static overall metrics, recalibrated overall metrics, and scalar deltas"
  artifacts:
    - path: "evaluate_twibot20.py"
      provides: "helper(s) or CLI support for writing a fresh evidence summary artifact from a live comparison run"
      contains: "transfer_evidence_summary"
    - path: "tests/test_evaluate_twibot20.py"
      provides: "tests for evidence summary structure or helper behavior"
      contains: "test_"
---

<objective>
Generate fresh live TwiBot comparison artifacts from the current revised adapter and capture the observed result in a stable evidence summary artifact.

Purpose: satisfy `EVID-01` by turning the Phase 11 reproducible command into current milestone evidence rather than stale root-level outputs.

Output:
- a milestone-owned evidence directory layout
- fresh comparison artifact generation support
- a machine-readable evidence summary extracted from the live comparison output
</objective>

<tasks>

<task>
  <name>Task 1: Add evidence-summary write path for a fresh TwiBot comparison run</name>
  <files>evaluate_twibot20.py</files>
  <action>
    Extend the TwiBot evaluation entry path so a fresh run can also emit a compact evidence summary artifact in the same output directory. The summary should be derived from `metrics_twibot20_comparison.json` and capture:
    - static overall metrics
    - recalibrated overall metrics
    - delta_overall
    - a short categorical interpretation such as `improved`, `worsened`, or `no_material_change`

    Keep the existing comparison artifact intact; this summary is an additional derivative output for Phase 12 evidence capture.
  </action>
  <verify>
    Add focused tests for the evidence-summary helper/shape without requiring a real model run.
  </verify>
</task>

<task>
  <name>Task 2: Make the fresh evidence directory explicit</name>
  <files>evaluate_twibot20.py</files>
  <action>
    Standardize a milestone-owned output directory convention for fresh transfer evidence so Phase 12 does not rely on ambiguous root-level artifacts. The code should make it straightforward to run:
    `python evaluate_twibot20.py test.json trained_system_v12.joblib <phase12_output_dir>`
    and know exactly which files should appear there.
  </action>
  <verify>
    Confirm the expected file list is documented or emitted clearly enough for downstream use.
  </verify>
</task>

</tasks>

<verification>
- `python -m py_compile evaluate_twibot20.py tests/test_evaluate_twibot20.py`
- targeted tests for the evidence summary helper
- if environment allows, a real TwiBot run into a dedicated Phase 12 output directory
</verification>

<success_criteria>
1. Phase 12 has a clear fresh-evidence output directory convention.
2. A live comparison run can produce a derivative evidence summary artifact from current static vs recalibrated metrics.
3. The evidence summary is stable enough for later docs and milestone reporting.
</success_criteria>
