---
phase: 11-reproducible-twibot-evaluation-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_evaluate_twibot20.py
autonomous: true
requirements:
  - REPRO-02
  - REPRO-03
must_haves:
  truths:
    - "Running `python -m pytest tests/test_evaluate_twibot20.py -q` reports 0 failed tests"
    - "test_evaluate_twibot20_returns_metrics passes when evaluate_twibot20() forwards online_calibration and window_size kwargs to run_inference"
    - "test_evaluate_twibot20_calls_evaluate_s3 passes when evaluate_twibot20() forwards online_calibration and window_size kwargs to run_inference"
    - "No production code in evaluate_twibot20.py is modified (the 21 existing passing tests still pass)"
  artifacts:
    - path: "tests/test_evaluate_twibot20.py"
      provides: "Fixed monkeypatch stubs that accept forwarded kwargs"
      contains: "lambda p, m, **kw: results_df"
  key_links:
    - from: "tests/test_evaluate_twibot20.py::test_evaluate_twibot20_returns_metrics"
      to: "evaluate_twibot20.evaluate_twibot20()"
      via: "monkeypatch.setattr on evaluate_twibot20.run_inference"
      pattern: "lambda p, m, \\*\\*kw: results_df"
    - from: "tests/test_evaluate_twibot20.py::test_evaluate_twibot20_calls_evaluate_s3"
      to: "evaluate_twibot20.evaluate_twibot20()"
      via: "monkeypatch.setattr on evaluate_twibot20.run_inference"
      pattern: "lambda p, m, \\*\\*kw: results_df"
---

<objective>
Fix the two pre-existing failing tests in tests/test_evaluate_twibot20.py so the full
pytest suite is green before Plan 02 hardens the artifact output path.

Purpose: The monkeypatched stubs for `evaluate_twibot20.run_inference` use a 2-argument
lambda `lambda p, m: results_df` that rejects the `online_calibration` and `window_size`
kwargs that evaluate_twibot20() forwards since Phase 9. This is a test-code bug, not a
production-code bug. Fixing it restores the green baseline required for REPRO-02
(no fragile defaults) verification in Plan 02.

Output: tests/test_evaluate_twibot20.py with both failing tests green; 111/111 tests pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/milestone/STATE.md
@.planning/workstreams/milestone/ROADMAP.md
@.planning/workstreams/milestone/phases/11-reproducible-twibot-evaluation-flow/11-RESEARCH.md
@tests/test_evaluate_twibot20.py
@evaluate_twibot20.py

<interfaces>
<!-- Production signature that the test stubs must match (DO NOT MODIFY). -->
<!-- Source: evaluate_twibot20.py lines 36-41, 188-201 -->

From evaluate_twibot20.py:
```python
def run_inference(
    path: str,
    model_path: str = "trained_system_v12.joblib",
    online_calibration: bool = True,
    window_size: int = 100,
) -> pd.DataFrame: ...

def evaluate_twibot20(
    path: str = "test.json",
    model_path: str = "trained_system_v12.joblib",
    threshold: float = 0.5,
    online_calibration: bool = True,
    window_size: int = 100,
) -> dict:
    """..."""
    results = run_inference(
        path,
        model_path,
        online_calibration=online_calibration,  # <-- forwarded kwarg
        window_size=window_size,                 # <-- forwarded kwarg
    )
```

The current test stubs at lines 267 and 310 are:
```python
monkeypatch.setattr("evaluate_twibot20.run_inference", lambda p, m: results_df)
```
These reject the forwarded kwargs and raise TypeError.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix lambda stub in test_evaluate_twibot20_returns_metrics</name>
  <files>tests/test_evaluate_twibot20.py</files>
  <read_first>
    - tests/test_evaluate_twibot20.py (see current broken stub at line 267 and passing-stub precedent at line 1002)
    - evaluate_twibot20.py (lines 188-201, confirm evaluate_twibot20 forwards online_calibration and window_size kwargs to run_inference)
    - .planning/workstreams/milestone/phases/11-reproducible-twibot-evaluation-flow/11-RESEARCH.md (Pattern 2, Pitfall 3)
  </read_first>
  <action>
    Open tests/test_evaluate_twibot20.py. On line 267, replace the exact string:
    `monkeypatch.setattr("evaluate_twibot20.run_inference", lambda p, m: results_df)`
    with:
    `monkeypatch.setattr("evaluate_twibot20.run_inference", lambda p, m, **kw: results_df)`

    This is a one-character-class addition (`, **kw`) that allows the stub to silently
    absorb the `online_calibration=False/True` and `window_size=100` kwargs that
    evaluate_twibot20() forwards since Phase 9.

    Do NOT modify any other line in this test. Do NOT modify evaluate_twibot20.py —
    per RESEARCH Pitfall 3 (line 247-258), the production code is correct; the test
    stub is what was narrow.
  </action>
  <verify>
    <automated>python -m pytest tests/test_evaluate_twibot20.py::test_evaluate_twibot20_returns_metrics -x --tb=short -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "lambda p, m, \*\*kw: results_df" tests/test_evaluate_twibot20.py` returns line 267 (the test_evaluate_twibot20_returns_metrics stub)
    - `grep -cn "lambda p, m: results_df" tests/test_evaluate_twibot20.py` does NOT match on line 267 (must be zero or only on other unrelated lines)
    - `python -m pytest tests/test_evaluate_twibot20.py::test_evaluate_twibot20_returns_metrics -x -q` exits 0 with output containing "1 passed"
    - Production file evaluate_twibot20.py is unchanged: `git diff --stat evaluate_twibot20.py` produces no output
  </acceptance_criteria>
  <done>
    test_evaluate_twibot20_returns_metrics passes; evaluate_twibot20.py is untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix lambda stub in test_evaluate_twibot20_calls_evaluate_s3</name>
  <files>tests/test_evaluate_twibot20.py</files>
  <read_first>
    - tests/test_evaluate_twibot20.py (see current broken stub at line 310 and the passing stub at line 1002 which correctly uses **kwargs)
    - evaluate_twibot20.py (confirm evaluate_twibot20 forwards kwargs to run_inference)
  </read_first>
  <action>
    Open tests/test_evaluate_twibot20.py. On line 310, replace the exact string:
    `monkeypatch.setattr("evaluate_twibot20.run_inference", lambda p, m: results_df)`
    with:
    `monkeypatch.setattr("evaluate_twibot20.run_inference", lambda p, m, **kw: results_df)`

    Same fix pattern as Task 1, applied to the second failing test. Both broken stubs
    sit in sibling tests that share the same failure mode (unexpected keyword argument
    from Phase 9 kwargs forwarding).

    Do NOT modify any other line in this test. Do NOT modify evaluate_twibot20.py.
  </action>
  <verify>
    <automated>python -m pytest tests/test_evaluate_twibot20.py::test_evaluate_twibot20_calls_evaluate_s3 -x --tb=short -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "lambda p, m, \*\*kw: results_df" tests/test_evaluate_twibot20.py` returns a count >= 2 (both Task 1 fix at line 267 and Task 2 fix at line 310)
    - `grep -c "lambda p, m: results_df" tests/test_evaluate_twibot20.py` returns 0 (neither broken stub remains)
    - `python -m pytest tests/test_evaluate_twibot20.py::test_evaluate_twibot20_calls_evaluate_s3 -x -q` exits 0 with output containing "1 passed"
    - Production file evaluate_twibot20.py is unchanged: `git diff --stat evaluate_twibot20.py` produces no output
  </acceptance_criteria>
  <done>
    test_evaluate_twibot20_calls_evaluate_s3 passes; both formerly-failing tests are green.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify full test suite is green</name>
  <files>tests/test_evaluate_twibot20.py</files>
  <read_first>
    - tests/test_evaluate_twibot20.py (verify both fixes landed)
  </read_first>
  <action>
    Run the full pytest suite and confirm that:
    - tests/test_evaluate_twibot20.py reports 0 failed (was 2 failed pre-fix)
    - tests/ overall reports 0 failed and 111 passed (was 2 failed / 109 passed pre-fix,
      per RESEARCH "Current Test State", lines 391-398)

    If any other test fails, stop and report the failure — do NOT attempt to fix
    unrelated failures in this plan. Plan 01 scope is strictly the two Phase-9 kwargs
    stub mismatches.

    Do NOT modify any production files in this task. This is verification only.
  </action>
  <verify>
    <automated>python -m pytest tests/ --tb=no -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_evaluate_twibot20.py --tb=no -q` exits 0 and output contains "0 failed" OR output does NOT contain "failed"
    - `python -m pytest tests/ --tb=no -q` output contains "111 passed" (or greater if new tests were added since RESEARCH was written, but zero failed)
    - Zero "FAILED" lines appear in the output of `python -m pytest tests/ --tb=no -q`
  </acceptance_criteria>
  <done>
    Full suite green; test baseline restored for Plan 02.
  </done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_evaluate_twibot20.py -q` exits 0 with 0 failed
- `python -m pytest tests/ --tb=no -q` exits 0 with 0 failed
- `git diff tests/test_evaluate_twibot20.py` shows exactly two `, **kw` insertions into
  the existing `lambda p, m: results_df` expressions, and nothing else
- `git diff evaluate_twibot20.py` produces no output (production code untouched)
</verification>

<success_criteria>
1. Both previously-failing tests in tests/test_evaluate_twibot20.py pass
2. Full pytest suite is green (0 failed)
3. Only tests/test_evaluate_twibot20.py is modified (no production code changes)
4. The two fixes are the minimal `**kw` additions described in RESEARCH.md Pattern 2
</success_criteria>

<output>
After completion, create `.planning/workstreams/milestone/phases/11-reproducible-twibot-evaluation-flow/11-01-SUMMARY.md`
</output>
