---
phase: 10-cross-domain-evaluation-and-paper-output
plan: 02
type: tdd
wave: 2
depends_on:
  - 10-01
files_modified:
  - ablation_tables.py
  - tests/test_ablation_tables.py
autonomous: true
requirements:
  - EVAL-02
must_haves:
  truths:
    - "The paper-ready cross-dataset table is generated from live BotSim-24 metrics plus the persisted TwiBot Phase 10 comparison artifact"
    - "The LaTeX table reflects both TwiBot conditions, not just one legacy zero-shot result"
    - "Table export remains compatible with the existing tables/ export flow"
  artifacts:
    - path: "ablation_tables.py"
      provides: "cross-dataset table builder for BotSim-24 plus both TwiBot conditions"
      contains: "def generate_cross_dataset_table("
    - path: "tests/test_ablation_tables.py"
      provides: "tests for the updated cross-dataset table shape and LaTeX output"
      contains: "test_"
---

<objective>
Update the paper-output path so the cross-dataset LaTeX table reflects the current milestone reality: BotSim-24 in-domain performance, TwiBot-20 static-threshold zero-shot performance, and TwiBot-20 online-recalibrated zero-shot performance.

Purpose: satisfy `EVAL-02` with a table that can drop directly into the paper robustness section.

Output: updated `ablation_tables.py` and tests, plus a stable `tables/` export path for the final cross-dataset table.
</objective>

<tasks>

<task>
  <name>Task 1: Expand the cross-dataset table builder</name>
  <files>ablation_tables.py</files>
  <action>
    Update `generate_cross_dataset_table()` so it accepts:
    - BotSim-24 metrics
    - TwiBot static-threshold metrics
    - TwiBot online-recalibrated metrics

    Return a DataFrame with headline rows:
    - F1
    - AUC-ROC
    - Precision
    - Recall

    and three metric columns:
    - `BotSim-24 (Reddit, in-dist.)`
    - `TwiBot-20 (Twitter, static)`
    - `TwiBot-20 (Twitter, recalibrated)`
  </action>
  <verify>
    Add tests that assert:
    - correct DataFrame shape
    - exact column labels
    - values are read from the `overall` metric block
  </verify>
</task>

<task>
  <name>Task 2: Wire the new comparison artifact into table generation</name>
  <files>ablation_tables.py</files>
  <action>
    Update `main()` so it loads the Phase 10 TwiBot comparison artifact(s), builds the new table, prints it, and exports the LaTeX file to `tables/table5_cross_dataset.tex`.
  </action>
  <verify>
    Add or update tests proving:
    - LaTeX export still works
    - the new table is skipped gracefully if the TwiBot Phase 10 artifact is absent
  </verify>
</task>

</tasks>

<verification>
- `python -m py_compile ablation_tables.py tests/test_ablation_tables.py`
- targeted pytest for the new table tests
- if environment allows, a real `python ablation_tables.py` run after Phase 10 metrics exist
</verification>

<success_criteria>
1. The cross-dataset table is generated from live Phase 10 artifacts rather than stale one-condition TwiBot metrics.
2. The LaTeX table includes BotSim-24, TwiBot static, and TwiBot recalibrated columns.
3. The export path remains `tables/table5_cross_dataset.tex`.
</success_criteria>
