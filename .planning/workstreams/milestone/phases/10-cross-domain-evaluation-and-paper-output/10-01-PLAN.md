---
phase: 10-cross-domain-evaluation-and-paper-output
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - evaluate_twibot20.py
  - tests/test_evaluate_twibot20.py
autonomous: true
requirements:
  - EVAL-01
must_haves:
  truths:
    - "Phase 10 can evaluate TwiBot-20 under both conditions using the live revised adapter: static thresholds and online recalibrated thresholds"
    - "A structured before/after comparison is produced without restoring the deprecated demographic-proxy adapter"
    - "The TwiBot comparison output is persisted to stable JSON artifact(s) for downstream paper-table generation"
    - "Existing single-condition evaluation helpers remain usable"
  artifacts:
    - path: "evaluate_twibot20.py"
      provides: "comparison-oriented TwiBot evaluation entry point and JSON persistence"
      contains: "def "
    - path: "tests/test_evaluate_twibot20.py"
      provides: "tests for TwiBot before/after comparison behavior"
      contains: "test_"
---

<objective>
Create the executable TwiBot Phase 10 comparison path. The code should run the current revised adapter in two modes - `online_calibration=False` and `online_calibration=True` - compute metrics for both via `evaluate_s3()`, print a readable before/after comparison, and persist the results to JSON for downstream table generation.

Purpose: satisfy `EVAL-01` using the current live milestone semantics rather than the superseded demographic-proxy adapter wording.

Output: updated `evaluate_twibot20.py` plus tests covering the comparison flow and artifact structure.
</objective>

<tasks>

<task>
  <name>Task 1: Add comparison helper and artifact persistence</name>
  <files>evaluate_twibot20.py</files>
  <action>
    Add a comparison-oriented helper in `evaluate_twibot20.py` that:
    1. runs `run_inference(..., online_calibration=False)`
    2. runs `run_inference(..., online_calibration=True, window_size=100)`
    3. evaluates both via `evaluate_s3()`
    4. returns a structured dict keyed by condition name
    5. optionally includes simple deltas for overall metrics

    Expand the script entry point so running `python evaluate_twibot20.py` can persist the two-condition metrics in a stable JSON artifact for Phase 10.
  </action>
  <verify>
    Add or update tests proving:
    - both conditions are evaluated
    - the output structure is JSON-serializable and stable
    - the comparison does not break the existing single-condition helper paths
  </verify>
</task>

<task>
  <name>Task 2: Add readable before/after console output</name>
  <files>evaluate_twibot20.py</files>
  <action>
    Print a compact summary that makes the before/after delta immediately readable at the command line. Reuse `evaluate_s3()` for detailed reporting, but add a concise explicit comparison block for:
    - F1
    - AUC
    - Precision
    - Recall
  </action>
  <verify>
    Confirm the output clearly distinguishes:
    - static thresholds
    - online recalibrated thresholds
  </verify>
</task>

</tasks>

<verification>
- `python -m py_compile evaluate_twibot20.py tests/test_evaluate_twibot20.py`
- targeted pytest for the new comparison tests
- if environment allows, a real TwiBot smoke run that writes the comparison artifact
</verification>

<success_criteria>
1. Phase 10 has a live TwiBot before/after comparison path using static vs online-recalibrated thresholds.
2. The comparison is saved to JSON in a stable machine-readable structure.
3. Existing single-condition evaluation helpers are preserved.
4. The console output makes the before/after change easy to read.
</success_criteria>

<notes>
- Assumption: in the current codebase, Phase 10's "before/after" means static-threshold revised adapter vs online-recalibrated revised adapter. The older demographic-proxy adapter is not reintroduced.
</notes>
