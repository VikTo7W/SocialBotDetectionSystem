---
phase: 22
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_train_botsim.py
  - tests/test_train_twibot.py
  - tests/test_evaluate.py
  - tests/test_api.py
  - tests/conftest.py
autonomous: true
requirements:
  - CONS-01
  - PRES-03
must_haves:
  truths:
    - "Tests pin `CascadePipeline` as the maintained orchestration surface"
    - "Compatibility use of `botdetector_pipeline.py` is explicit and temporary rather than an equal maintained path"
    - "Routing, gating, and prediction-shape parity are protected before implementation moves begin"
---

# Plan 22-01

## Objective

Create the red-test safety net for pipeline-surface consolidation before moving helpers or demoting wrappers.

## Tasks

1. Update tests so the maintained training and evaluation callers are asserted against `CascadePipeline` rather than ambiguous dual ownership.
2. Add or tighten compatibility tests around `botdetector_pipeline.train_system()` / `predict_system()` so they are clearly treated as forwarding wrappers, not a second orchestration implementation.
3. Add parity-focused assertions around routing masks, prediction columns, and AMR-only behavior that would catch accidental drift during helper relocation.

## Verification

- `python -m py_compile tests/test_train_botsim.py tests/test_train_twibot.py tests/test_evaluate.py tests/test_api.py tests/conftest.py`
- targeted pytest for the updated pipeline-surface and compatibility tests

## Acceptance Criteria

- Tests clearly identify one maintained pipeline surface.
- Compatibility behavior is preserved where active callers still depend on it.
- Any red-state failures point at real ownership overlap rather than broader feature or I/O work from later phases.
