---
phase: 22
plan: 02
type: execute
wave: 1
depends_on:
  - 22-01-PLAN.md
files_modified:
  - cascade_pipeline.py
  - botdetector_pipeline.py
autonomous: true
requirements:
  - CONS-01
  - PRES-03
must_haves:
  truths:
    - "`cascade_pipeline.py` becomes the clear orchestration source of truth"
    - "`botdetector_pipeline.py` no longer looks like an equal maintained pipeline implementation"
    - "Split discipline, AMR-only Stage 2b behavior, gating logic, and meta-model semantics stay intact"
---

# Plan 22-02

## Objective

Consolidate orchestration ownership into the maintained pipeline surface and demote the legacy module to compatibility or stage-support duties only.

## Tasks

1. Move pipeline-level helpers to the owned surface that best matches their responsibility, favoring `cascade_pipeline.py` for orchestration concerns.
2. Simplify `botdetector_pipeline.py` so it re-exports or forwards only what active callers still need, without preserving a competing end-to-end implementation story.
3. Keep the public compatibility functions stable where they are still used, but make their forwarding behavior obvious in code structure and comments.

## Verification

- `python -m py_compile cascade_pipeline.py botdetector_pipeline.py`
- targeted pytest for training/evaluation compatibility and pipeline parity coverage

## Acceptance Criteria

- One file is plainly the maintained pipeline owner.
- The other file no longer contains duplicated orchestration responsibility.
- Existing routing and prediction behavior remain unchanged at the contract level.
