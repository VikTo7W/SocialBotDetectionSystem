---
phase: 14-twitter-native-feature-pipeline
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - features_stage2_twitter.py
  - tests/test_features_stage2_twitter.py
autonomous: true
requirements:
  - TWN-02
must_haves:
  truths:
    - "Stage 2 features are extracted only from TwiBot-native tweet text/timeline inputs"
    - "Timestamp-derived Reddit transfer workarounds are not reused as fake in-distribution values in the TwiBot-native Stage 2 path"
    - "The native Stage 2 output contract is documented and stable for empty-message and multi-message accounts"
    - "This plan does not reuse `features_stage2.py` unchanged for the native TwiBot path"
  artifacts:
    - path: "features_stage2_twitter.py"
      provides: "standalone TwiBot-native Stage 2 feature extractor"
      contains: "extract_stage2_features_twitter"
    - path: "tests/test_features_stage2_twitter.py"
      provides: "unit coverage for TwiBot-native Stage 2 behavior"
      contains: "test_"
---

<objective>
Implement the TwiBot-native Stage 2 feature extractor so the future supervised TwiBot cascade uses a feature vector that honestly reflects TwiBot tweet-text availability instead of the Reddit transfer compatibility path.

Purpose: satisfy `TWN-02` with a native tweet-text feature pipeline whose behavior is verified independently of any full model training run.

Output:
- `features_stage2_twitter.py`
- Stage 2 unit tests proving shape and no-message behavior
</objective>

<tasks>

<task>
  <name>Task 1: Build the standalone TwiBot-native Stage 2 extractor</name>
  <files>features_stage2_twitter.py</files>
  <action>
    Create a new standalone Stage 2 extractor for TwiBot-native tweet content. Reuse the project’s established embedding and lightweight linguistic/similarity patterns where appropriate, but do not reuse the Reddit transfer sentinel strategy or the existing Reddit extractor unchanged as a fake native signal path.

    The extractor should clearly document its output dimensionality and its behavior when accounts have:
    - no messages
    - one message
    - multiple near-duplicate messages
  </action>
  <verify>
    The extractor must produce a stable numeric matrix from the TwiBot `messages` shape without requiring Reddit-only assumptions.
  </verify>
</task>

<task>
  <name>Task 2: Add focused Stage 2 unit coverage</name>
  <files>tests/test_features_stage2_twitter.py</files>
  <action>
    Add targeted tests using small synthetic TwiBot message lists and a fake embedder so the native Stage 2 path can be verified cheaply. Cover:
    - empty-message accounts
    - single-message accounts
    - repeated/near-duplicate message behavior
    - shape and dtype stability
  </action>
  <verify>
    Tests should assert the expected output shape and at least a few key feature values rather than only smoke-testing that the function runs.
  </verify>
</task>

</tasks>

<verification>
- `python -m py_compile features_stage2_twitter.py tests/test_features_stage2_twitter.py`
- targeted pytest for `tests/test_features_stage2_twitter.py`
</verification>

<success_criteria>
1. A standalone native Stage 2 extractor exists for TwiBot tweet text.
2. The native Stage 2 contract does not depend on Reddit transfer-only assumptions.
3. Unit tests lock down the extractor’s shape and edge-case behavior.
</success_criteria>
