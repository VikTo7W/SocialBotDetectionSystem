---
phase: 14-twitter-native-feature-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - features_stage1_twitter.py
  - tests/test_features_stage1_twitter.py
autonomous: true
requirements:
  - TWN-01
must_haves:
  truths:
    - "Stage 1 features are extracted only from TwiBot-native account/activity fields already exposed by twibot20_io.load_accounts()"
    - "No Reddit slot mapping logic is imported or reused in the TwiBot-native Stage 1 extractor"
    - "The extractor produces a documented stable numeric matrix for both normal and edge-case TwiBot accounts"
  artifacts:
    - path: "features_stage1_twitter.py"
      provides: "standalone TwiBot-native Stage 1 feature extractor"
      contains: "extract_stage1_matrix_twitter"
    - path: "tests/test_features_stage1_twitter.py"
      provides: "unit coverage of TwiBot-native Stage 1 feature behavior"
      contains: "test_"
---

<objective>
Implement the TwiBot-native Stage 1 feature extractor so the future TwiBot-trained cascade no longer depends on Reddit schema analogs for metadata/account behavior.

Purpose: satisfy `TWN-01` with a standalone feature path that consumes only TwiBot-native account/activity inputs and is independently testable before any model training begins.

Output:
- `features_stage1_twitter.py`
- Stage 1 unit tests proving shape and feature behavior
</objective>

<tasks>

<task>
  <name>Task 1: Build the standalone TwiBot-native Stage 1 extractor</name>
  <files>features_stage1_twitter.py</files>
  <action>
    Create a new standalone module for TwiBot-native Stage 1 feature extraction. The extractor should consume the normalized DataFrame shape produced by `twibot20_io.load_accounts()` and use only TwiBot-native signals such as screen-name properties, account counts, created-at-derived age/rates, domain breadth, and tweet-type breakdowns from `parse_tweet_types()`.

    Do not import or branch from `features_stage1.py`. The new extractor should define and document its own stable output contract.
  </action>
  <verify>
    Prove the extractor returns a numeric matrix with a stable column order and does not require Reddit-only columns.
  </verify>
</task>

<task>
  <name>Task 2: Add focused Stage 1 unit coverage</name>
  <files>tests/test_features_stage1_twitter.py</files>
  <action>
    Add unit tests for representative TwiBot account cases, including:
    - a typical account with valid counts and created_at
    - zero-tweet and zero-domain edge cases
    - RT-heavy behavior that changes tweet-type-derived features
    - malformed or empty created_at handling if the implementation chooses an explicit fallback contract
  </action>
  <verify>
    Tests should assert both matrix shape and a handful of semantically meaningful feature values so future refactors cannot silently drift the feature contract.
  </verify>
</task>

</tasks>

<verification>
- `python -m py_compile features_stage1_twitter.py tests/test_features_stage1_twitter.py`
- targeted pytest for `tests/test_features_stage1_twitter.py`
</verification>

<success_criteria>
1. A standalone Stage 1 extractor exists for TwiBot-native metadata/account behavior.
2. The extractor uses only fields genuinely available from the TwiBot loader.
3. Its feature contract is locked down by direct unit tests.
</success_criteria>
