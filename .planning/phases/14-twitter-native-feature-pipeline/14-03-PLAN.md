---
phase: 14-twitter-native-feature-pipeline
plan: 03
type: execute
wave: 2
depends_on:
  - 14-01
  - 14-02
files_modified:
  - features_stage3_twitter.py
  - tests/test_features_stage3_twitter.py
autonomous: true
requirements:
  - TWN-03
must_haves:
  truths:
    - "The TwiBot-native Stage 3 path has an explicit, testable graph feature contract rather than an implicit dependency on the generic builder alone"
    - "The wrapper/helper consumes only TwiBot-native edge/account inputs produced by twibot20_io and botdetector_pipeline graph utilities"
    - "The output contract documents how unused Twitter-absent edge-type dimensions are handled in the native path"
  artifacts:
    - path: "features_stage3_twitter.py"
      provides: "TwiBot-native Stage 3 graph feature wrapper/helper"
      contains: "extract_stage3_features_twitter"
    - path: "tests/test_features_stage3_twitter.py"
      provides: "unit coverage for the Stage 3 native graph contract"
      contains: "test_"
---

<objective>
Define the TwiBot-native Stage 3 graph feature contract so the training phase can consume a documented, tested Twitter-native structural feature path rather than relying on undocumented assumptions about the generic graph builder.

Purpose: satisfy `TWN-03` by making the native graph feature path explicit and independently verifiable before full TwiBot training begins.

Output:
- `features_stage3_twitter.py`
- Stage 3 unit tests for TwiBot-native graph feature behavior
</objective>

<tasks>

<task>
  <name>Task 1: Implement the TwiBot-native Stage 3 wrapper/helper</name>
  <files>features_stage3_twitter.py</files>
  <action>
    Create a small TwiBot-native Stage 3 wrapper/helper that calls the existing graph feature machinery but publishes a clear native feature contract for TwiBot accounts and edges.

    This wrapper should make the native path explicit for future Phase 15 training code, including how the TwiBot-native graph contract treats the absent third edge-type block.
  </action>
  <verify>
    The helper should be callable directly from TwiBot-native account/edge inputs and produce a documented stable matrix shape.
  </verify>
</task>

<task>
  <name>Task 2: Add focused Stage 3 graph tests</name>
  <files>tests/test_features_stage3_twitter.py</files>
  <action>
    Add synthetic graph tests covering:
    - empty-edge accounts
    - simple following/follower patterns
    - the intended handling of the absent third edge type in the native contract
  </action>
  <verify>
    Tests should assert both matrix shape and at least a few concrete structural values so later training code can treat this helper as a stable dependency.
  </verify>
</task>

</tasks>

<verification>
- `python -m py_compile features_stage3_twitter.py tests/test_features_stage3_twitter.py`
- targeted pytest for `tests/test_features_stage3_twitter.py`
</verification>

<success_criteria>
1. The TwiBot-native Stage 3 path is explicit, documented, and testable.
2. The native graph feature contract is no longer implicit in generic pipeline internals alone.
3. Phase 15 can depend on the Stage 3 helper without redefining graph semantics.
</success_criteria>
