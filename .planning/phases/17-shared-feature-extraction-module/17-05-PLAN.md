---
phase: 17
plan: 05
type: execute
wave: 1
depends_on:
  - 17-01
files_modified:
  - botdetector_pipeline.py
  - main.py
  - tests/conftest.py
  - tests/test_calibrate.py
  - tests/test_lstm_removed.py
autonomous: true
requirements:
  - CORE-05
must_haves:
  truths:
    - "No LSTM Stage 2b symbols remain in botdetector_pipeline.py"
    - "TrainedSystem no longer advertises LSTM-only fields or variant switching"
    - "The AMR-only path remains wired in both training and prediction code"
  artifacts:
    - path: "botdetector_pipeline.py"
      provides: "AMR-only Stage 2b pipeline contract"
      contains: "class TrainedSystem"
    - path: "tests/test_lstm_removed.py"
      provides: "assertions that LSTM symbols and dataclass fields are gone"
      contains: "stage2b_lstm"
    - path: "tests/test_calibrate.py"
      provides: "updated calibration tests aligned to the AMR-only pipeline"
      contains: "test_predict_system_uses_amr_branch_when_configured"
---

<objective>
Remove the LSTM Stage 2b path and simplify the shared pipeline contract to AMR-only.

Purpose: satisfy CORE-05 by deleting the LSTM refiner classes, variant switching helpers,
and dataclass fields from the pipeline while keeping the AMR delta-logit path intact for
both training and inference.

Output:
- simplified `botdetector_pipeline.py`
- cleaned integration points in `main.py`
- updated tests proving the LSTM path is gone
</objective>

<tasks>

<task>
  <name>Task 1: Delete LSTM-specific symbols and variant infrastructure from the pipeline</name>
  <files>
    - botdetector_pipeline.py
    - tests/test_lstm_removed.py
  </files>
  <action>
    Remove the LSTM-only code paths from `botdetector_pipeline.py`, including:
    - `_Stage2LSTMNet`
    - `Stage2LSTMRefiner`
    - LSTM sequence helpers
    - variant normalization / switching helpers
    - `stage2b_lstm` and `stage2b_variant` from `TrainedSystem`

    Inline or simplify the AMR-only refinement path where needed so training and prediction
    still work without a variant layer.
  </action>
  <verify>
    Prove the dedicated LSTM-removal tests pass and that `TrainedSystem` no longer exposes
    the removed fields.
  </verify>
</task>

<task>
  <name>Task 2: Clean up remaining LSTM references in entry-point and test code</name>
  <files>
    - main.py
    - tests/conftest.py
    - tests/test_calibrate.py
  </files>
  <action>
    Remove stale LSTM imports and assumptions from `main.py`, shared test fixtures, and
    calibration tests. Preserve the AMR-path assertions and any non-LSTM calibration behavior.
  </action>
  <verify>
    Prove the updated files import cleanly and no longer reference removed LSTM symbols.
  </verify>
</task>

</tasks>

<verification>
- `python -m py_compile botdetector_pipeline.py main.py tests/conftest.py tests/test_calibrate.py tests/test_lstm_removed.py`
- `python -m pytest tests/test_lstm_removed.py tests/test_calibrate.py -x -q`
- `python -c "import botdetector_pipeline as bp; import dataclasses; print('stage2b_lstm' in {f.name for f in dataclasses.fields(bp.TrainedSystem)})"`
</verification>

<success_criteria>
1. The Stage 2b implementation is AMR-only at the pipeline level.
2. No LSTM-specific symbols, dataclass fields, or test fixtures remain in the maintained code path.
3. The AMR-only refinement contract remains wired for later Phase 18 pipeline unification.
</success_criteria>
