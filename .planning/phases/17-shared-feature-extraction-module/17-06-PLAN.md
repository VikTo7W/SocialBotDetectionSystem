---
phase: 17
plan: 06
type: execute
wave: 2
depends_on:
  - 17-02
  - 17-03
  - 17-04
  - 17-05
files_modified:
  - features_stage1.py
  - features_stage1_twitter.py
  - features_stage2.py
  - features_stage2_twitter.py
  - features_stage3_twitter.py
autonomous: true
requirements:
  - CORE-01
  - CORE-02
  - CORE-05
must_haves:
  truths:
    - "Legacy feature modules no longer act as the source of truth for extraction logic"
    - "Phase 17 ends with a coherent import surface that supports both the new shared package and the still-legacy callers that remain until Phase 18"
    - "Whole-phase verification covers the new package, the AMR-only pipeline, and the updated tests together"
  artifacts:
    - path: "features_stage1.py"
      provides: "legacy compatibility shim or deprecation bridge"
      contains: "deprecated"
    - path: "features_stage2.py"
      provides: "legacy compatibility shim or deprecation bridge"
      contains: "deprecated"
    - path: "features_stage1_twitter.py"
      provides: "legacy twibot compatibility shim or deprecation bridge"
      contains: "deprecated"
---

<objective>
Close the phase with compatibility bridges and whole-phase verification.

Purpose: finish Phase 17 by making the new shared `features/` package the source of truth
while preserving safe compatibility for any still-legacy imports that are not scheduled to
move until Phase 18, then verify the refactor as one coherent phase.

Output:
- legacy feature modules downgraded to compatibility shims or explicit deprecation layers
- whole-phase verification evidence for CORE-01, CORE-02, and CORE-05
</objective>

<tasks>

<task>
  <name>Task 1: Convert legacy feature modules into thin compatibility layers</name>
  <files>
    - features_stage1.py
    - features_stage1_twitter.py
    - features_stage2.py
    - features_stage2_twitter.py
    - features_stage3_twitter.py
  </files>
  <action>
    Ensure the old feature modules are no longer the source of truth. Replace their bodies
    with thin wrappers, forwarding imports, or explicit deprecation shims that delegate to
    the new `features.*` package while preserving the legacy call surfaces needed before
    Phase 18 updates all remaining pipeline callers.
  </action>
  <verify>
    Prove legacy imports still resolve cleanly while the new package remains the single
    maintained implementation surface.
  </verify>
</task>

<task>
  <name>Task 2: Run phase-level verification and capture integration confidence</name>
  <files>
    - tests/test_features_stage1.py
    - tests/test_data_io.py
    - tests/test_features_stage2.py
    - tests/test_features_stage2_twitter.py
    - tests/test_features_stage3_twitter.py
    - tests/test_lstm_removed.py
  </files>
  <action>
    Run the whole set of Phase 17 validation targets together and fix any integration issues
    caused by the package transition or the AMR-only cleanup.
  </action>
  <verify>
    Prove the Phase 17 unit and integration checks run cleanly enough to hand off to Phase 18,
    acknowledging the existing Windows tmp-dir test-environment caveat if it still appears.
  </verify>
</task>

</tasks>

<verification>
- `python -m py_compile features_stage1.py features_stage1_twitter.py features_stage2.py features_stage2_twitter.py features_stage3_twitter.py`
- `python -m pytest tests/test_features_stage1.py tests/test_data_io.py tests/test_features_stage2.py tests/test_features_stage2_twitter.py tests/test_features_stage3_twitter.py tests/test_lstm_removed.py -x -q`
- `python -m pytest tests/ -q`
</verification>

<success_criteria>
1. The new shared `features/` package is the maintained source of truth for extraction logic.
2. Legacy feature imports remain safe bridges until Phase 18 updates the remaining callers.
3. Phase 17 ends with integrated verification coverage across shared extraction, shared loading, and LSTM removal.
</success_criteria>
