---
phase: 17
plan: 02
type: execute
wave: 1
depends_on:
  - 17-01
files_modified:
  - features/__init__.py
  - features/stage1.py
  - data_io.py
  - tests/test_features_stage1.py
  - tests/test_data_io.py
autonomous: true
requirements:
  - CORE-01
  - CORE-02
must_haves:
  truths:
    - "Stage1Extractor exposes a dataset-validated class interface and preserves the exact botsim and twibot output contracts"
    - "load_dataset(dataset, ...) dispatches cleanly to botsim and twibot loaders without embedding dataset-specific branching into callers"
    - "The new features package is importable without pulling in pipeline or training modules"
  artifacts:
    - path: "features/stage1.py"
      provides: "dataset-parameterized Stage1Extractor for botsim and twibot"
      contains: "class Stage1Extractor"
    - path: "data_io.py"
      provides: "top-level dataset dispatch for shared loaders"
      contains: "def load_dataset"
    - path: "tests/test_features_stage1.py"
      provides: "shape and validation coverage for Stage1Extractor"
      contains: "test_stage1_"
    - path: "tests/test_data_io.py"
      provides: "dispatch contract coverage for load_dataset"
      contains: "load_dataset"
---

<objective>
Build the shared Stage 1 and dataset-loading foundation for the unified modular codebase.

Purpose: satisfy the Phase 17 CORE-01 and CORE-02 requirements for the Stage 1 surface area by
introducing the new `features/` package, implementing `Stage1Extractor(dataset=...)`, and adding
the new `data_io.load_dataset()` dispatch without changing any training or evaluation entry points.

Output:
- `features/__init__.py`
- `features/stage1.py`
- `data_io.py`
- green Stage 1 and data I/O tests
</objective>

<tasks>

<task>
  <name>Task 1: Implement Stage1Extractor in the new features package</name>
  <files>
    - features/__init__.py
    - features/stage1.py
    - tests/test_features_stage1.py
  </files>
  <action>
    Create the new `features/` package and implement `Stage1Extractor(dataset)` with an
    `extract(df, reference_time=None)` method.

    Preserve the exact botsim and twibot output contracts from the existing procedural
    extractors:
    - botsim path must keep the 10-column float32 matrix behavior from `features_stage1.py`
    - twibot path must keep the 14-column float32 matrix behavior and
      `STAGE1_TWITTER_COLUMNS` contract from `features_stage1_twitter.py`

    Keep dataset branching internal to the class. Reject unknown dataset values with a
    clear `ValueError`.
  </action>
  <verify>
    Prove the new class preserves the expected shapes, dtypes, empty-frame behavior, and
    NaN sanitization pinned by `tests/test_features_stage1.py`.
  </verify>
</task>

<task>
  <name>Task 2: Implement unified dataset dispatch in data_io.py</name>
  <files>
    - data_io.py
    - tests/test_data_io.py
  </files>
  <action>
    Create `data_io.py` with a top-level `load_dataset(dataset, **kwargs)` function and
    private `_load_botsim(...)` / `_load_twibot(...)` helpers.

    Wrap the existing public loaders from `botsim24_io.py` and `twibot20_io.py` instead of
    copying their internals. Preserve their current return contracts so downstream code can
    adopt the new dispatch incrementally in later phases.
  </action>
  <verify>
    Prove the dispatch entry point is importable, callable, and rejects unknown dataset
    values exactly as the new tests require.
  </verify>
</task>

</tasks>

<verification>
- `python -m py_compile features/stage1.py data_io.py tests/test_features_stage1.py tests/test_data_io.py`
- `python -m pytest tests/test_features_stage1.py tests/test_data_io.py -x -q`
- `python -c "from features.stage1 import Stage1Extractor; from data_io import load_dataset; print('imports ok')"`
</verification>

<success_criteria>
1. `features/` exists and can be imported without dragging in unrelated pipeline code.
2. `Stage1Extractor('botsim')` and `Stage1Extractor('twibot')` preserve the exact existing Stage 1 output contracts.
3. `load_dataset()` becomes the shared top-level dataset dispatch surface for later phases.
</success_criteria>
