---
phase: 17
plan: 04
type: execute
wave: 1
depends_on:
  - 17-01
files_modified:
  - features/stage3.py
  - features_stage3_twitter.py
  - tests/test_features_stage3_twitter.py
autonomous: true
requirements:
  - CORE-02
must_haves:
  truths:
    - "Stage 3 graph feature extraction lives under features/ rather than in the pipeline module"
    - "The relocated graph builder preserves the existing 18-column contract and remains directly importable"
    - "Existing twibot Stage 3 tests verify the new shared interface rather than the legacy twitter-only wrapper"
  artifacts:
    - path: "features/stage3.py"
      provides: "shared Stage 3 extractor and relocated graph builder"
      contains: "class Stage3Extractor"
    - path: "tests/test_features_stage3_twitter.py"
      provides: "contract coverage for the shared Stage 3 extractor"
      contains: "Stage3Extractor"
---

<objective>
Move Stage 3 feature extraction into the shared features package.

Purpose: satisfy the Stage 3 portion of CORE-02 by relocating the graph feature builder out
of `botdetector_pipeline.py`, wrapping it in `Stage3Extractor(dataset)`, and preserving the
existing twibot graph-feature contract.

Output:
- `features/stage3.py`
- updated Stage 3 tests
- legacy wrapper aligned to the new module location
</objective>

<tasks>

<task>
  <name>Task 1: Relocate build_graph_features_nodeidx into features/stage3.py</name>
  <files>
    - features/stage3.py
  </files>
  <action>
    Move `build_graph_features_nodeidx` into `features/stage3.py` as a module-level export
    and implement `Stage3Extractor(dataset)` around it.

    Preserve the existing graph feature contract, including:
    - the 18-column twibot output layout
    - the current `n_types=3` default
    - the ability to compute `num_nodes_total` when it is omitted
  </action>
  <verify>
    Prove the relocated builder returns the same shape and dtype expectations as the old
    implementation and remains directly importable for downstream pipeline use.
  </verify>
</task>

<task>
  <name>Task 2: Align the legacy twitter wrapper and tests to the shared Stage 3 module</name>
  <files>
    - features_stage3_twitter.py
    - tests/test_features_stage3_twitter.py
  </files>
  <action>
    Update the existing twibot-facing wrapper and tests so they target the shared Stage 3
    implementation. The legacy module may remain as a compatibility shim in this phase, but
    its source of truth must become `features.stage3`.
  </action>
  <verify>
    Prove the updated Stage 3 tests exercise the shared extractor surface and no longer depend
    on `botdetector_pipeline.py` as the graph-feature source.
  </verify>
</task>

</tasks>

<verification>
- `python -m py_compile features/stage3.py features_stage3_twitter.py tests/test_features_stage3_twitter.py`
- `python -m pytest tests/test_features_stage3_twitter.py -x -q`
- `python -c "from features.stage3 import Stage3Extractor, build_graph_features_nodeidx; print('stage3 import ok')"`
</verification>

<success_criteria>
1. Stage 3 extraction code lives in `features/stage3.py`.
2. The graph builder remains importable as a shared utility for later pipeline refactors.
3. The Stage 3 tests confirm the shared interface preserves the existing twibot graph feature contract.
</success_criteria>
