---
phase: 17
plan: 03
type: execute
wave: 1
depends_on:
  - 17-01
files_modified:
  - features/stage2.py
  - tests/test_features_stage2.py
  - tests/test_features_stage2_twitter.py
autonomous: true
requirements:
  - CORE-02
must_haves:
  truths:
    - "Stage2Extractor preserves both the botsim 397-dim contract and the twibot 393-dim contract under one dataset-parameterized class"
    - "AMR embedding extraction is exposed from the shared Stage 2 module rather than via a separate LSTM-oriented utility surface"
    - "Dataset-specific temporal behavior stays internal to the extractor class"
  artifacts:
    - path: "features/stage2.py"
      provides: "shared Stage2Extractor and AMR embedding extraction"
      contains: "class Stage2Extractor"
    - path: "tests/test_features_stage2.py"
      provides: "botsim Stage 2 contract coverage through the new class"
      contains: "Stage2Extractor"
    - path: "tests/test_features_stage2_twitter.py"
      provides: "twibot Stage 2 contract coverage through the new class"
      contains: "Stage2Extractor"
---

<objective>
Implement the shared Stage 2 extractor surface for both datasets.

Purpose: satisfy the Stage 2 portion of CORE-02 by replacing the duplicated procedural
Stage 2 extractors with a single `Stage2Extractor(dataset)` class that preserves both
dataset-specific output contracts and exposes the AMR extraction helper needed by the
pipeline.

Output:
- `features/stage2.py`
- updated Stage 2 tests for botsim and twibot
</objective>

<tasks>

<task>
  <name>Task 1: Implement dataset-parameterized Stage2Extractor</name>
  <files>
    - features/stage2.py
    - tests/test_features_stage2.py
    - tests/test_features_stage2_twitter.py
  </files>
  <action>
    Implement `Stage2Extractor(dataset)` with:
    - `extract(df, embedder, max_msgs=50, max_chars=500)`
    - `extract_amr(df, embedder, max_chars=500)`

    Preserve the exact existing output contracts:
    - botsim: 397-dim float32 vector with the timestamp-sentinel behavior retained
    - twibot: 393-dim float32 vector with the twitter-native column set retained

    Keep the dataset branching private to the class. Do not add a variant system or any
    LSTM-specific utilities.
  </action>
  <verify>
    Prove the class satisfies the updated botsim and twibot Stage 2 tests, including the
    pinned dimensionality, helper behavior, and column-order assumptions.
  </verify>
</task>

<task>
  <name>Task 2: Relocate AMR embedding extraction to the shared Stage 2 module</name>
  <files>
    - features/stage2.py
  </files>
  <action>
    Move or re-home the AMR embedding extraction logic into the shared Stage 2 module so
    feature extraction responsibilities live together. Keep the behavior aligned with the
    existing AMR path and avoid introducing any new coupling to training or calibration code.
  </action>
  <verify>
    Prove the shared Stage 2 module can be imported and that `extract_amr(...)` returns
    the expected embedding-shaped output on synthetic inputs.
  </verify>
</task>

</tasks>

<verification>
- `python -m py_compile features/stage2.py tests/test_features_stage2.py tests/test_features_stage2_twitter.py`
- `python -m pytest tests/test_features_stage2.py tests/test_features_stage2_twitter.py -x -q`
- `python -c "from features.stage2 import Stage2Extractor; print('stage2 import ok')"`
</verification>

<success_criteria>
1. A single `Stage2Extractor` class supports both botsim and twibot via `dataset=`.
2. The botsim and twibot Stage 2 contracts remain byte-for-byte compatible at the feature-matrix level.
3. The shared Stage 2 module exposes the AMR extraction helper needed by the AMR-only pipeline path.
</success_criteria>
