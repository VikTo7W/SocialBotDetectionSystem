---
phase: 20
plan: 03
type: execute
wave: 1
depends_on:
  - 20-01
files_modified:
  - eval_reddit_twibot_transfer.py
  - tests/test_eval_reddit_twibot_transfer.py
autonomous: true
requirements:
  - EVAL-02
  - PAPER-01
  - PAPER-02
must_haves:
  truths:
    - "`python eval_reddit_twibot_transfer.py` loads `trained_system_botsim.joblib` (NOT `trained_system_v12.joblib`) and runs zero-shot transfer inference on TwiBot-20 test.json"
    - "Column mapping from TwiBot schema to BotSim schema happens via DataFrame rewrite (`_apply_transfer_adapter`), NOT via monkey-patching `bp.extract_stage1_matrix`"
    - "Inference routes through `CascadePipeline('botsim').predict()`"
    - "The script writes `paper_outputs/metrics_reddit_transfer.json` and `paper_outputs/confusion_matrix_reddit_transfer.png`"
    - "`botdetector_pipeline.extract_stage1_matrix` is unchanged after the script runs (no global state mutation)"
  artifacts:
    - path: "eval_reddit_twibot_transfer.py"
      provides: "Maintained Reddit-trained-to-TwiBot-transfer evaluation entry point"
      exports: ["DEFAULT_OUTPUT_DIR", "_apply_transfer_adapter", "run_inference_transfer", "evaluate_reddit_twibot_transfer", "_write_confusion_matrix", "_save_json"]
  key_links:
    - from: "eval_reddit_twibot_transfer.py"
      to: "train_botsim.DEFAULT_BOTSIM_MODEL_PATH"
      via: "direct import for default model path"
      pattern: "from train_botsim import DEFAULT_BOTSIM_MODEL_PATH"
    - from: "eval_reddit_twibot_transfer.py"
      to: "twibot20_io.parse_tweet_types"
      via: "used in `_apply_transfer_adapter` to compute submission_num, comment_num_1, comment_num_2"
      pattern: "parse_tweet_types"
    - from: "eval_reddit_twibot_transfer.py"
      to: "cascade_pipeline.CascadePipeline"
      via: "runs inference with dataset='botsim'"
      pattern: "CascadePipeline\\(\"botsim\""
---

<objective>
Create `eval_reddit_twibot_transfer.py` as the maintained EVAL-02 entry point. The script loads the Reddit-trained `trained_system_botsim.joblib` and evaluates it zero-shot on TwiBot-20 `test.json` by rewriting TwiBot columns to match the BotSim Stage 1 schema BEFORE calling `CascadePipeline("botsim").predict()`. This replaces the legacy `evaluate_twibot20.py` monkey-patch pattern on `bp.extract_stage1_matrix`.

Purpose: turn the Wave 0 red tests for EVAL-02, PAPER-01, PAPER-02 green, and eliminate global-state mutation. Runs in parallel with Plans 20-02 and 20-04 (no file overlap).

Output: one new production module plus a pinned-green test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-evaluation-entry-points-and-paper-outputs/20-RESEARCH.md
@.planning/phases/20-evaluation-entry-points-and-paper-outputs/20-PATTERNS.md
@.planning/phases/20-evaluation-entry-points-and-paper-outputs/20-01-SUMMARY.md

@evaluate_twibot20.py
@evaluate_twibot20_native.py
@train_botsim.py
@twibot20_io.py
@features/stage1.py
@cascade_pipeline.py

<interfaces>
<!-- Contracts consumed by eval_reddit_twibot_transfer.py. -->

Stage 1 botsim extractor reads these named columns (from features/stage1.py lines 46-61):
    df["username"]          (string)
    df["submission_num"]    (float)
    df["comment_num_1"]     (float)
    df["comment_num_2"]     (float)
    df["subreddit_list"]    (list[str])
The adapter MUST populate all five. Ratios (post/comment, post/subreddit) are
computed inside extract() with eps=1e-6 and NaN/Inf are already sanitized by
np.nan_to_num — no external clamping needed.

Imports (top of file):
    from __future__ import annotations
    import json, os, sys
    import joblib
    import matplotlib
    matplotlib.use("Agg")
    import matplotlib.pyplot as plt
    import numpy as np
    import pandas as pd
    from sklearn.metrics import ConfusionMatrixDisplay

    from cascade_pipeline import CascadePipeline
    from evaluate import evaluate_s3
    from train_botsim import DEFAULT_BOTSIM_MODEL_PATH
    from twibot20_io import build_edges, load_accounts, parse_tweet_types

Module constants:
    DEFAULT_OUTPUT_DIR = "paper_outputs"
    DEFAULT_TEST_JSON_PATH = "test.json"

Function signatures:
    def _apply_transfer_adapter(accounts_df: pd.DataFrame) -> pd.DataFrame:
        """Return a NEW DataFrame with BotSim Stage 1 columns populated from TwiBot fields.
        Does not mutate the input."""

    def run_inference_transfer(
        path: str,
        model_path: str = DEFAULT_BOTSIM_MODEL_PATH,
    ) -> tuple[pd.DataFrame, pd.DataFrame]:
        """Return (results_df, adapted_accounts_df)."""

    def evaluate_reddit_twibot_transfer(
        path: str = DEFAULT_TEST_JSON_PATH,
        model_path: str = DEFAULT_BOTSIM_MODEL_PATH,
        output_dir: str = DEFAULT_OUTPUT_DIR,
        threshold: float = 0.5,
    ) -> dict:
        """Return {"metrics": {...}, "paths": {"metrics", "confusion_matrix", "model"}}"""

Legacy column-mapping logic (extract verbatim from evaluate_twibot20.py lines 168-175):
    tweet_stats = [parse_tweet_types(msgs) for msgs in df["messages"]]
    df["submission_num"] = [float(s["original_count"] + s["mt_count"] + s["rt_count"]) for s in tweet_stats]
    df["comment_num_1"] = [float(s["original_count"]) for s in tweet_stats]
    df["comment_num_2"] = [float(s["mt_count"]) for s in tweet_stats]
    df["subreddit_list"] = df["domain_list"].tolist()

Additional aliasing required: TwiBot accounts carry `screen_name` but not
`username`. The BotSim Stage1Extractor reads `df["username"]`. The adapter
must add `username` by aliasing from `screen_name` when `username` is absent.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement `_apply_transfer_adapter` and `run_inference_transfer`</name>
  <files>eval_reddit_twibot_transfer.py</files>
  <read_first>
    - evaluate_twibot20.py lines 1-278 (legacy transfer adapter — lift column mapping, do NOT lift `bp.extract_stage1_matrix` monkey-patch)
    - evaluate_twibot20_native.py lines 1-85 (structural template for run_inference + evaluate)
    - twibot20_io.py (for `load_accounts`, `build_edges`, `parse_tweet_types` — confirm DataFrames carry `messages`, `domain_list`, `screen_name`, `label`)
    - features/stage1.py lines 30-80 (confirm named column set Stage1Extractor("botsim") reads)
    - tests/test_eval_reddit_twibot_transfer.py (Wave 0 contract tests — every assertion must pass by end of Task 2)
  </read_first>
  <behavior>
    - `from eval_reddit_twibot_transfer import _apply_transfer_adapter, run_inference_transfer, DEFAULT_OUTPUT_DIR` succeeds.
    - `_apply_transfer_adapter(accounts_df)` returns a NEW DataFrame with all of `username`, `submission_num`, `comment_num_1`, `comment_num_2`, `subreddit_list` populated.
    - `_apply_transfer_adapter` does not mutate the caller's DataFrame (caller's `accounts_df.columns` does not gain `submission_num`).
    - `run_inference_transfer(path, model_path)` calls `joblib.load(model_path)` exactly once, instantiates `CascadePipeline("botsim", cfg=system.cfg, embedder=system.embedder)`, and returns `(results, adapted_accounts_df)`.
    - `botdetector_pipeline.extract_stage1_matrix` is unchanged at import time and after `run_inference_transfer` returns.
  </behavior>
  <action>
    Create `eval_reddit_twibot_transfer.py`. Use the exact imports and constants from the `<interfaces>` block above. Full Task 1 content:

    Top-of-file: `from __future__ import annotations` then imports (in order):
    `json`, `os`, `sys`, `joblib`, `matplotlib` (immediately followed by `matplotlib.use("Agg")` BEFORE `import matplotlib.pyplot as plt`), `numpy as np`, `pandas as pd`, `from sklearn.metrics import ConfusionMatrixDisplay`, `from cascade_pipeline import CascadePipeline`, `from evaluate import evaluate_s3`, `from train_botsim import DEFAULT_BOTSIM_MODEL_PATH`, `from twibot20_io import build_edges, load_accounts, parse_tweet_types`.

    Define `DEFAULT_OUTPUT_DIR = "paper_outputs"` and `DEFAULT_TEST_JSON_PATH = "test.json"` at module level.

    Define four helper functions (paste directly):

    `_save_json(payload, path)` — opens `path` with `encoding="utf-8"`, writes `json.dump(payload, f, indent=2)`.

    `_write_confusion_matrix(results, y_true, threshold, output_path, title)` — computes `y_pred = (results["p_final"].to_numpy() >= threshold).astype(int)`, builds `ConfusionMatrixDisplay.from_predictions(y_true, y_pred, display_labels=["human", "bot"], colorbar=False, cmap="Blues")`, sets the title via `disp.ax_.set_title(title)`, calls `disp.figure_.tight_layout()` + `disp.figure_.savefig(output_path, dpi=150, bbox_inches="tight")`, then `plt.close(disp.figure_)`.

    `_apply_transfer_adapter(accounts_df)`:
    - `df = accounts_df.copy()`
    - `tweet_stats = [parse_tweet_types(msgs) for msgs in df["messages"]]`
    - `df["submission_num"] = [float(s["original_count"] + s["mt_count"] + s["rt_count"]) for s in tweet_stats]`
    - `df["comment_num_1"] = [float(s["original_count"]) for s in tweet_stats]`
    - `df["comment_num_2"] = [float(s["mt_count"]) for s in tweet_stats]`
    - `df["subreddit_list"] = df["domain_list"].tolist()`
    - `if "username" not in df.columns and "screen_name" in df.columns: df["username"] = df["screen_name"]`
    - `return df`

    `run_inference_transfer(path, model_path=DEFAULT_BOTSIM_MODEL_PATH) -> tuple[pd.DataFrame, pd.DataFrame]`:
    - `accounts_df = load_accounts(path)`
    - `adapted = _apply_transfer_adapter(accounts_df)`
    - `edges_df = build_edges(adapted, path)`
    - `system = joblib.load(model_path)`
    - `pipeline = CascadePipeline("botsim", cfg=system.cfg, embedder=system.embedder)`
    - `results = pipeline.predict(system, df=adapted, edges_df=edges_df, nodes_total=len(adapted))`
    - `return results, adapted`

    Do NOT import `botdetector_pipeline as bp`. Do NOT assign to `bp.extract_stage1_matrix` anywhere. Do NOT replicate the legacy ratio-percentile stdout logging from `evaluate_twibot20.py` lines 201-228 — it produces no paper artifact.

    Do NOT add `evaluate_reddit_twibot_transfer` or `__main__` yet; Task 2 covers those.

    Commit message: `feat(20): add eval_reddit_twibot_transfer adapter without bp monkey-patch`
  </action>
  <verify>
    <automated>python -m py_compile eval_reddit_twibot_transfer.py &amp;&amp; python scripts/check_no_bp_patch.py eval_reddit_twibot_transfer</automated>
  </verify>
  <acceptance_criteria>
    - `eval_reddit_twibot_transfer.py` exists and `python -m py_compile eval_reddit_twibot_transfer.py` exits 0.
    - `grep "from train_botsim import DEFAULT_BOTSIM_MODEL_PATH" eval_reddit_twibot_transfer.py` returns a match.
    - `grep "import botdetector_pipeline" eval_reddit_twibot_transfer.py` returns NO matches (anti-regression).
    - `grep "bp.extract_stage1_matrix" eval_reddit_twibot_transfer.py` returns NO matches.
    - `grep "_RATIO_CAP" eval_reddit_twibot_transfer.py` returns NO matches (no legacy clamping).
    - `grep "CascadePipeline(\"botsim\"" eval_reddit_twibot_transfer.py` returns a match.
    - `grep "DEFAULT_OUTPUT_DIR = \"paper_outputs\"" eval_reddit_twibot_transfer.py` returns a match.
    - `grep -n "matplotlib.use" eval_reddit_twibot_transfer.py` reports a line number BEFORE the `import matplotlib.pyplot` line.
    - Inline Python check (run as a one-liner, no external script file needed):
      `python -c "import botdetector_pipeline as bp; before = bp.extract_stage1_matrix; import eval_reddit_twibot_transfer; assert bp.extract_stage1_matrix is before; print('no-patch OK')"` exits 0.
      (If the automated command above cannot be invoked directly, substitute this inline check in the executor's terminal; do not create a scripts/ helper file.)
  </acceptance_criteria>
  <done>Adapter module imports cleanly, returns a new DataFrame with all five BotSim Stage 1 columns, and does NOT mutate `botdetector_pipeline` module state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement `evaluate_reddit_twibot_transfer`, CLI `__main__`, and turn Wave 0 tests green</name>
  <files>eval_reddit_twibot_transfer.py</files>
  <read_first>
    - eval_reddit_twibot_transfer.py (the Task 1 skeleton)
    - evaluate_twibot20.py lines 382-408 (legacy `__main__` — use as CLI template, but write to `paper_outputs/`, not the Phase 16 artifact dir)
    - evaluate.py (confirm evaluate_s3 signature)
    - tests/test_eval_reddit_twibot_transfer.py (every assertion must pass)
  </read_first>
  <behavior>
    After this task:
    - `python -m pytest tests/test_eval_reddit_twibot_transfer.py -x -q` passes every test.
    - `evaluate_reddit_twibot_transfer(path, model_path, output_dir=tmp_dir)` returns `{"metrics": {...}, "paths": {"metrics", "confusion_matrix", "model"}}`.
    - The function writes `metrics_reddit_transfer.json` and `confusion_matrix_reddit_transfer.png` under `output_dir`.
    - `botdetector_pipeline.extract_stage1_matrix` identity check still holds after a full call.
  </behavior>
  <action>
    Append to `eval_reddit_twibot_transfer.py`:

    `evaluate_reddit_twibot_transfer(path=DEFAULT_TEST_JSON_PATH, model_path=DEFAULT_BOTSIM_MODEL_PATH, output_dir=DEFAULT_OUTPUT_DIR, threshold=0.5) -> dict`:
    - `os.makedirs(output_dir, exist_ok=True)`
    - `results, adapted = run_inference_transfer(path, model_path=model_path)`
    - `y_true = adapted["label"].to_numpy()`
    - `metrics = evaluate_s3(results, y_true, threshold=threshold, verbose=True)`
    - `metrics_path = os.path.join(output_dir, "metrics_reddit_transfer.json")`
    - `cm_path = os.path.join(output_dir, "confusion_matrix_reddit_transfer.png")`
    - `_save_json(metrics, metrics_path)`
    - `_write_confusion_matrix(results, y_true, threshold, cm_path, title="Reddit-transfer (TwiBot-20) confusion matrix")`
    - `return {"metrics": metrics, "paths": {"metrics": metrics_path, "confusion_matrix": cm_path, "model": model_path}}`

    CLI block (at bottom of file):

    ```
    if __name__ == "__main__":
        data_path = sys.argv[1] if len(sys.argv) > 1 else DEFAULT_TEST_JSON_PATH
        model_path = sys.argv[2] if len(sys.argv) > 2 else DEFAULT_BOTSIM_MODEL_PATH
        output_dir = sys.argv[3] if len(sys.argv) > 3 else DEFAULT_OUTPUT_DIR
        summary = evaluate_reddit_twibot_transfer(
            path=data_path, model_path=model_path, output_dir=output_dir,
        )
        print(f"[reddit-transfer] model:            {summary['paths']['model']}")
        print(f"[reddit-transfer] metrics:          {summary['paths']['metrics']}")
        print(f"[reddit-transfer] confusion_matrix: {summary['paths']['confusion_matrix']}")
    ```

    Run `python -m pytest tests/test_eval_reddit_twibot_transfer.py -x -q` and iterate on production code (NOT tests) until green. Do NOT edit `tests/test_eval_reddit_twibot_transfer.py` — tests are the contract.

    Commit message: `feat(20): complete eval_reddit_twibot_transfer evaluate + CLI`
  </action>
  <verify>
    <automated>python -m pytest tests/test_eval_reddit_twibot_transfer.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_eval_reddit_twibot_transfer.py -x -q` exits 0.
    - `grep "metrics_reddit_transfer.json" eval_reddit_twibot_transfer.py` returns a match.
    - `grep "confusion_matrix_reddit_transfer.png" eval_reddit_twibot_transfer.py` returns a match.
    - `grep 'if __name__ == "__main__"' eval_reddit_twibot_transfer.py` returns a match.
    - `grep "os.makedirs(output_dir" eval_reddit_twibot_transfer.py` returns a match.
    - Pre-existing tests still green: `python -m pytest tests/test_evaluate.py tests/test_evaluate_twibot20.py tests/test_evaluate_twibot20_native.py -x -q` exits 0 OR the only failure is the pre-existing `test_evaluate_twibot20.test_run_inference_returns_correct_schema` failure noted in RESEARCH.md (document it in the SUMMARY; do not fix it in this plan).
  </acceptance_criteria>
  <done>Every Wave 0 test for EVAL-02 passes; zero-shot transfer runs without patching `botdetector_pipeline` module state; paper outputs land in `paper_outputs/`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Local filesystem only: reads TwiBot `test.json`, reads a joblib artifact, writes `metrics_reddit_transfer.json` and `confusion_matrix_reddit_transfer.png` under `paper_outputs/`. No network I/O, no user auth, no session handling.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-03 | N/A | local file read/write only | accept | ASVS L1 categories do not apply — no network, no user input beyond filesystem paths in `sys.argv`. |
| T-20-04 | Tampering | prior global-state monkey-patch of `botdetector_pipeline.extract_stage1_matrix` | mitigate | Replaced with DataFrame column rewrite in `_apply_transfer_adapter`; `botdetector_pipeline` module is not imported in this file. Enforced by acceptance grep + runtime identity check. |
</threat_model>

<verification>
- `python -m pytest tests/test_eval_reddit_twibot_transfer.py -x -q` exits 0
- `python -m py_compile eval_reddit_twibot_transfer.py` exits 0
- `botdetector_pipeline.extract_stage1_matrix` identity is preserved across import and call
</verification>

<success_criteria>
- `eval_reddit_twibot_transfer.py` is callable from `python eval_reddit_twibot_transfer.py`.
- Writes `paper_outputs/metrics_reddit_transfer.json` with `evaluate_s3()` dict shape.
- Writes `paper_outputs/confusion_matrix_reddit_transfer.png`.
- Loads `trained_system_botsim.joblib` (NOT `trained_system_v12.joblib`).
- Never mutates `botdetector_pipeline` module state.
</success_criteria>

<output>
After completion, create `.planning/phases/20-evaluation-entry-points-and-paper-outputs/20-03-SUMMARY.md`.
</output>
