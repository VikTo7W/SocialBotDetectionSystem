---
phase: 20
plan: 04
type: execute
wave: 1
depends_on:
  - 20-01
files_modified:
  - eval_twibot_native.py
  - tests/test_eval_twibot_native.py
autonomous: true
requirements:
  - EVAL-03
  - PAPER-01
  - PAPER-02
must_haves:
  truths:
    - "`python eval_twibot_native.py` loads `trained_system_twibot.joblib` and evaluates on TwiBot-20 `test.json`"
    - "Inference routes through `CascadePipeline('twibot').predict()`"
    - "The script writes `paper_outputs/metrics_twibot_native.json` and `paper_outputs/confusion_matrix_twibot_native.png`"
    - "The script never loads `trained_system_botsim.joblib` or `trained_system_v12.joblib`"
  artifacts:
    - path: "eval_twibot_native.py"
      provides: "Maintained TwiBot-native evaluation entry point"
      exports: ["DEFAULT_OUTPUT_DIR", "run_inference_twibot_native", "evaluate_twibot_native", "_write_confusion_matrix", "_save_json"]
  key_links:
    - from: "eval_twibot_native.py"
      to: "train_twibot.DEFAULT_TWIBOT_MODEL_PATH"
      via: "direct import for default model path"
      pattern: "from train_twibot import DEFAULT_TWIBOT_MODEL_PATH"
    - from: "eval_twibot_native.py"
      to: "train_twibot.load_accounts_with_ids"
      via: "loads TwiBot accounts with stable account_id column"
      pattern: "load_accounts_with_ids"
    - from: "eval_twibot_native.py"
      to: "cascade_pipeline.CascadePipeline"
      via: "runs inference with dataset='twibot'"
      pattern: "CascadePipeline\\(\"twibot\""
---

<objective>
Create `eval_twibot_native.py` as the maintained EVAL-03 entry point. The script loads the TwiBot-trained `trained_system_twibot.joblib` and evaluates it on TwiBot-20 `test.json` through `CascadePipeline("twibot").predict()`. It supersedes the legacy `evaluate_twibot20_native.py` and writes paper outputs to `paper_outputs/` (instead of the Phase 15 artifact directory).

Purpose: turn the Wave 0 red tests for EVAL-03, PAPER-01, PAPER-02 green. Runs in parallel with Plans 20-02 and 20-03 (zero file overlap).

Output: one new production module plus a pinned-green test file. The legacy `evaluate_twibot20_native.py` is NOT deleted or demoted in this plan — Phase 20 keeps it as-is so pre-existing tests do not regress; a future phase may reduce it to a shim.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-evaluation-entry-points-and-paper-outputs/20-RESEARCH.md
@.planning/phases/20-evaluation-entry-points-and-paper-outputs/20-PATTERNS.md
@.planning/phases/20-evaluation-entry-points-and-paper-outputs/20-01-SUMMARY.md

@evaluate_twibot20_native.py
@train_twibot.py
@evaluate.py
@cascade_pipeline.py

<interfaces>
<!-- Contracts consumed by eval_twibot_native.py. Near-verbatim from evaluate_twibot20_native.py. -->

Imports (top of file):
    from __future__ import annotations
    import json, os, sys
    import joblib
    import matplotlib
    matplotlib.use("Agg")
    import matplotlib.pyplot as plt
    import pandas as pd
    from sklearn.metrics import ConfusionMatrixDisplay

    from cascade_pipeline import CascadePipeline
    from evaluate import evaluate_s3
    from train_twibot import DEFAULT_TWIBOT_MODEL_PATH, load_accounts_with_ids
    from twibot20_io import build_edges

Module constants:
    DEFAULT_OUTPUT_DIR = "paper_outputs"
    DEFAULT_TEST_JSON_PATH = "test.json"

Function signatures:
    def run_inference_twibot_native(
        path: str,
        model_path: str = DEFAULT_TWIBOT_MODEL_PATH,
    ) -> pd.DataFrame:
        """Return results_df with cascade columns."""

    def evaluate_twibot_native(
        path: str = DEFAULT_TEST_JSON_PATH,
        model_path: str = DEFAULT_TWIBOT_MODEL_PATH,
        output_dir: str = DEFAULT_OUTPUT_DIR,
        threshold: float = 0.5,
    ) -> dict:
        """Return {"metrics": {...}, "paths": {"metrics", "confusion_matrix", "model"}}"""

Existing run_inference_native in evaluate_twibot20_native.py lines 29-42 is the
near-verbatim source pattern. The only differences in this new module:
  - dataset label "twibot" is the same
  - output filenames change to metrics_twibot_native.json / confusion_matrix_twibot_native.png
  - default output_dir changes from PHASE15_ARTIFACT_DIR to "paper_outputs"
  - a confusion-matrix writer is added (PAPER-01 is new in v1.5)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement `eval_twibot_native.py` with `run_inference_twibot_native` and helpers</name>
  <files>eval_twibot_native.py</files>
  <read_first>
    - evaluate_twibot20_native.py (entire file — the structural template)
    - train_twibot.py lines 1-60 (for DEFAULT_TWIBOT_MODEL_PATH, load_accounts_with_ids)
    - twibot20_io.py (confirm `build_edges(accounts_df, path)` signature)
    - cascade_pipeline.py (confirm CascadePipeline init signature)
    - tests/test_eval_twibot_native.py (Wave 0 red tests)
  </read_first>
  <behavior>
    - `from eval_twibot_native import run_inference_twibot_native, DEFAULT_OUTPUT_DIR, _write_confusion_matrix, _save_json` succeeds.
    - `run_inference_twibot_native(path, model_path="X")` calls `load_accounts_with_ids(path)`, `build_edges(accounts_df, path)`, `joblib.load("X")`, instantiates `CascadePipeline("twibot", cfg=system.cfg, embedder=system.embedder)`, and returns the DataFrame produced by `pipeline.predict(...)`.
    - `matplotlib.use("Agg")` appears before `import matplotlib.pyplot as plt`.
  </behavior>
  <action>
    Create `eval_twibot_native.py` with the imports from `<interfaces>`.

    Define constants:
    - `DEFAULT_OUTPUT_DIR = "paper_outputs"`
    - `DEFAULT_TEST_JSON_PATH = "test.json"`

    Define `_save_json(payload, path)` — same two-line helper as in `evaluate_twibot20_native.py` lines 24-26.

    Define `_write_confusion_matrix(results, y_true, threshold, output_path, title)` — identical body to Plan 20-02 Task 1: compute `y_pred = (results["p_final"].to_numpy() >= threshold).astype(int)`, call `ConfusionMatrixDisplay.from_predictions(y_true, y_pred, display_labels=["human", "bot"], colorbar=False, cmap="Blues")`, set title via `disp.ax_.set_title(title)`, call `disp.figure_.tight_layout()` + `disp.figure_.savefig(output_path, dpi=150, bbox_inches="tight")`, finally `plt.close(disp.figure_)`.

    Define `run_inference_twibot_native`:
    - `accounts_df = load_accounts_with_ids(path)`
    - `edges_df = build_edges(accounts_df, path)`
    - `system = joblib.load(model_path)`
    - `pipeline = CascadePipeline("twibot", cfg=system.cfg, embedder=system.embedder)`
    - `return pipeline.predict(system, df=accounts_df, edges_df=edges_df, nodes_total=len(accounts_df))`

    Do NOT add `evaluate_twibot_native` or `__main__` yet — Task 2 covers those.

    Do NOT import anything from `evaluate_twibot20_native.py`; this is a fresh entry point.

    Commit message: `feat(20): add eval_twibot_native run_inference`
  </action>
  <verify>
    <automated>python -m py_compile eval_twibot_native.py &amp;&amp; python -c "from eval_twibot_native import run_inference_twibot_native, DEFAULT_OUTPUT_DIR, _write_confusion_matrix, _save_json; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `eval_twibot_native.py` exists and `python -m py_compile eval_twibot_native.py` exits 0.
    - `grep "from train_twibot import DEFAULT_TWIBOT_MODEL_PATH, load_accounts_with_ids" eval_twibot_native.py` returns a match (or the same two imports on separate lines — both are acceptable).
    - `grep "from twibot20_io import build_edges" eval_twibot_native.py` returns a match.
    - `grep "CascadePipeline(\"twibot\"" eval_twibot_native.py` returns a match.
    - `grep "DEFAULT_OUTPUT_DIR = \"paper_outputs\"" eval_twibot_native.py` returns a match.
    - `grep -n "matplotlib.use" eval_twibot_native.py` returns a line BEFORE the `import matplotlib.pyplot` line.
    - `grep "from evaluate_twibot20_native" eval_twibot_native.py` returns NO matches (fresh entry point, no re-export from legacy).
    - `python -c "import eval_twibot_native; assert eval_twibot_native.DEFAULT_OUTPUT_DIR == 'paper_outputs'"` exits 0.
  </acceptance_criteria>
  <done>Module imports cleanly; `run_inference_twibot_native` routes through `CascadePipeline("twibot")` and loads `DEFAULT_TWIBOT_MODEL_PATH` by default.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement `evaluate_twibot_native`, CLI `__main__`, and turn Wave 0 tests green</name>
  <files>eval_twibot_native.py</files>
  <read_first>
    - eval_twibot_native.py (Task 1 skeleton)
    - evaluate_twibot20_native.py lines 45-85 (evaluate_* and __main__ template)
    - tests/test_eval_twibot_native.py (every assertion must pass)
  </read_first>
  <behavior>
    - `python -m pytest tests/test_eval_twibot_native.py -x -q` passes every test.
    - `evaluate_twibot_native(path, model_path, output_dir=tmp_dir)` returns `{"metrics": {...}, "paths": {"metrics", "confusion_matrix", "model"}}`.
    - Writes `metrics_twibot_native.json` and `confusion_matrix_twibot_native.png` under `output_dir`.
  </behavior>
  <action>
    Append `evaluate_twibot_native` to `eval_twibot_native.py`:

    `evaluate_twibot_native(path=DEFAULT_TEST_JSON_PATH, model_path=DEFAULT_TWIBOT_MODEL_PATH, output_dir=DEFAULT_OUTPUT_DIR, threshold=0.5) -> dict`:
    - `os.makedirs(output_dir, exist_ok=True)`
    - `results = run_inference_twibot_native(path, model_path=model_path)`
    - `accounts_df = load_accounts_with_ids(path)`  (re-load; matches legacy pattern)
    - `y_true = accounts_df["label"].to_numpy()`
    - `metrics = evaluate_s3(results, y_true, threshold=threshold, verbose=True)`
    - `metrics_path = os.path.join(output_dir, "metrics_twibot_native.json")`
    - `cm_path = os.path.join(output_dir, "confusion_matrix_twibot_native.png")`
    - `_save_json(metrics, metrics_path)`
    - `_write_confusion_matrix(results, y_true, threshold, cm_path, title="TwiBot-20 native confusion matrix")`
    - `return {"metrics": metrics, "paths": {"metrics": metrics_path, "confusion_matrix": cm_path, "model": model_path}}`

    CLI block:

        if __name__ == "__main__":
            data_path = sys.argv[1] if len(sys.argv) > 1 else DEFAULT_TEST_JSON_PATH
            model_path = sys.argv[2] if len(sys.argv) > 2 else DEFAULT_TWIBOT_MODEL_PATH
            output_dir = sys.argv[3] if len(sys.argv) > 3 else DEFAULT_OUTPUT_DIR
            summary = evaluate_twibot_native(
                path=data_path, model_path=model_path, output_dir=output_dir,
            )
            print(f"[twibot-native] model:            {summary['paths']['model']}")
            print(f"[twibot-native] metrics:          {summary['paths']['metrics']}")
            print(f"[twibot-native] confusion_matrix: {summary['paths']['confusion_matrix']}")

    Run `python -m pytest tests/test_eval_twibot_native.py -x -q` and iterate on production code (NOT tests) until green. Do NOT edit `tests/test_eval_twibot_native.py`.

    Commit message: `feat(20): complete eval_twibot_native evaluate + CLI with paper outputs`
  </action>
  <verify>
    <automated>python -m pytest tests/test_eval_twibot_native.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_eval_twibot_native.py -x -q` exits 0.
    - `grep "metrics_twibot_native.json" eval_twibot_native.py` returns a match.
    - `grep "confusion_matrix_twibot_native.png" eval_twibot_native.py` returns a match.
    - `grep 'if __name__ == "__main__"' eval_twibot_native.py` returns a match.
    - `grep "trained_system_botsim" eval_twibot_native.py` returns NO matches (cross-dataset anti-regression).
    - `grep "trained_system_v12" eval_twibot_native.py` returns NO matches.
    - Pre-existing tests remain green: `python -m pytest tests/test_evaluate_twibot20_native.py tests/test_evaluate.py -x -q` exits 0.
  </acceptance_criteria>
  <done>Every Wave 0 test for EVAL-03 passes; TwiBot-native evaluation writes `paper_outputs/metrics_twibot_native.json` and `paper_outputs/confusion_matrix_twibot_native.png` through the shared cascade.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Local filesystem only: reads TwiBot `test.json`, reads `trained_system_twibot.joblib`, writes `metrics_twibot_native.json` and `confusion_matrix_twibot_native.png` under `paper_outputs/`. No network, no auth, no user input beyond filesystem paths.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-05 | N/A | local file read/write only | accept | ASVS L1 categories do not apply — no network, no user input beyond filesystem paths in `sys.argv`. |
</threat_model>

<verification>
- `python -m pytest tests/test_eval_twibot_native.py -x -q` exits 0
- `python -m py_compile eval_twibot_native.py` exits 0
- Pre-existing `tests/test_evaluate_twibot20_native.py` remains green
</verification>

<success_criteria>
- `eval_twibot_native.py` is callable from `python eval_twibot_native.py`.
- Writes `paper_outputs/metrics_twibot_native.json` with `evaluate_s3()` dict shape.
- Writes `paper_outputs/confusion_matrix_twibot_native.png`.
- Loads `trained_system_twibot.joblib` by default; never touches `trained_system_botsim.joblib` or `trained_system_v12.joblib`.
</success_criteria>

<output>
After completion, create `.planning/phases/20-evaluation-entry-points-and-paper-outputs/20-04-SUMMARY.md`.
</output>
