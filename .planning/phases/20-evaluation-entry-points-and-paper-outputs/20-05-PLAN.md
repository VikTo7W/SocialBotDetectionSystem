---
phase: 20
plan: 05
type: execute
wave: 2
depends_on:
  - 20-01
  - 20-02
  - 20-03
  - 20-04
files_modified:
  - generate_table5.py
  - tests/test_paper_outputs.py
autonomous: true
requirements:
  - PAPER-03
must_haves:
  truths:
    - "`python generate_table5.py` reads three persisted metrics JSON files from `paper_outputs/` and writes `tables/table5_cross_dataset.tex` without manual steps"
    - "The driver calls `ablation_tables.generate_cross_dataset_table` with the correct argument order (BotSim, Reddit-transfer, TwiBot-native)"
    - "The driver calls `ablation_tables.save_latex` to persist the LaTeX output"
    - "Default input paths point to the Phase 20 output locations, NOT the stale Phase 15/16 artifact directories referenced by `ablation_tables.DEFAULT_NATIVE_METRICS_PATH`"
  artifacts:
    - path: "generate_table5.py"
      provides: "Standalone PAPER-03 Table 5 driver"
      exports: ["DEFAULT_BOTSIM_METRICS_PATH", "DEFAULT_REDDIT_TRANSFER_METRICS_PATH", "DEFAULT_TWIBOT_NATIVE_METRICS_PATH", "DEFAULT_TABLE5_OUTPUT_PATH", "generate_table5", "_load_metrics"]
  key_links:
    - from: "generate_table5.py"
      to: "ablation_tables.generate_cross_dataset_table"
      via: "reuses existing three-column table builder"
      pattern: "from ablation_tables import generate_cross_dataset_table"
    - from: "generate_table5.py"
      to: "ablation_tables.save_latex"
      via: "reuses existing LaTeX writer"
      pattern: "save_latex"
    - from: "paper_outputs/metrics_botsim.json"
      to: "generate_table5.py"
      via: "read at default BotSim metrics path"
      pattern: "paper_outputs.*metrics_botsim.json"
---

<objective>
Create `generate_table5.py` as the maintained PAPER-03 driver. The standalone script reads `paper_outputs/metrics_botsim.json`, `paper_outputs/metrics_reddit_transfer.json`, and `paper_outputs/metrics_twibot_native.json` (produced by Plans 20-02 / 20-03 / 20-04), calls `ablation_tables.generate_cross_dataset_table`, and writes `tables/table5_cross_dataset.tex` via `ablation_tables.save_latex`. No edits to `ablation_tables.py` are needed — the existing generator is reused as-is.

Purpose: turn the Wave 0 red tests for PAPER-03 green, and close the end-to-end paper-output loop so running `eval_botsim_native.py` + `eval_reddit_twibot_transfer.py` + `eval_twibot_native.py` + `generate_table5.py` produces every artifact the paper requires with zero manual steps.

Output: one new production module. The Wave 0 test file (`tests/test_paper_outputs.py`) is turned green by this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-evaluation-entry-points-and-paper-outputs/20-RESEARCH.md
@.planning/phases/20-evaluation-entry-points-and-paper-outputs/20-PATTERNS.md
@.planning/phases/20-evaluation-entry-points-and-paper-outputs/20-01-SUMMARY.md
@.planning/phases/20-evaluation-entry-points-and-paper-outputs/20-02-SUMMARY.md
@.planning/phases/20-evaluation-entry-points-and-paper-outputs/20-03-SUMMARY.md
@.planning/phases/20-evaluation-entry-points-and-paper-outputs/20-04-SUMMARY.md

@ablation_tables.py

<interfaces>
<!-- Contracts consumed by generate_table5.py. -->

From ablation_tables.py lines 192-229 (stable since v1.4):
    def generate_cross_dataset_table(
        botsim24_metrics: dict,         # full evaluate_s3() return dict
        reddit_transfer_metrics: dict,  # full evaluate_s3() return dict
        twibot20_native_metrics: dict,  # full evaluate_s3() return dict
    ) -> pd.DataFrame
    # returns DataFrame with columns:
    #   ["Metric", "BotSim-24 (Reddit, in-dist.)",
    #    "TwiBot-20 (Reddit transfer)", "TwiBot-20 (TwiBot-native)"]
    # 4 rows: F1, AUC-ROC, Precision, Recall

From ablation_tables.py lines 375-388:
    def save_latex(df: pd.DataFrame, path: str) -> None:
        # creates parent dir if missing; writes df.to_latex(...) to `path`.

Default input file shape (written by Plans 20-02 / 20-03 / 20-04):
    {
      "overall":   {"f1", "auc", "precision", "recall"},
      "per_stage": {"p1": {...}, "p2": {...}, "p12": {...}, "p_final": {...}},
      "routing":   {"pct_stage1_exit", "pct_stage2_exit",
                    "pct_stage3_exit", "pct_amr_triggered"}
    }

Imports (top of file):
    from __future__ import annotations
    import json, os, sys
    from ablation_tables import generate_cross_dataset_table, save_latex

Module constants:
    DEFAULT_BOTSIM_METRICS_PATH          = os.path.join("paper_outputs", "metrics_botsim.json")
    DEFAULT_REDDIT_TRANSFER_METRICS_PATH = os.path.join("paper_outputs", "metrics_reddit_transfer.json")
    DEFAULT_TWIBOT_NATIVE_METRICS_PATH   = os.path.join("paper_outputs", "metrics_twibot_native.json")
    DEFAULT_TABLE5_OUTPUT_PATH           = os.path.join("tables", "table5_cross_dataset.tex")

Function signatures:
    def _load_metrics(path: str) -> dict: ...  # reads JSON, raises FileNotFoundError if missing

    def generate_table5(
        botsim_metrics_path: str = DEFAULT_BOTSIM_METRICS_PATH,
        reddit_transfer_metrics_path: str = DEFAULT_REDDIT_TRANSFER_METRICS_PATH,
        twibot_native_metrics_path: str = DEFAULT_TWIBOT_NATIVE_METRICS_PATH,
        output_path: str = DEFAULT_TABLE5_OUTPUT_PATH,
    ) -> str:
        """Load three metrics JSON files, build Table 5, save as LaTeX, return output_path."""
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement `generate_table5.py` with file-existence precondition checks</name>
  <files>generate_table5.py</files>
  <read_first>
    - ablation_tables.py lines 192-229, 375-388 (generate_cross_dataset_table and save_latex — both are already tested and stable)
    - tests/test_paper_outputs.py (every assertion must pass — implementation is derived from the tests)
    - .planning/phases/20-evaluation-entry-points-and-paper-outputs/20-02-SUMMARY.md through 20-04-SUMMARY.md (confirm the three metrics JSON filenames landed as planned)
  </read_first>
  <behavior>
    - `from generate_table5 import generate_table5, DEFAULT_BOTSIM_METRICS_PATH, DEFAULT_REDDIT_TRANSFER_METRICS_PATH, DEFAULT_TWIBOT_NATIVE_METRICS_PATH, DEFAULT_TABLE5_OUTPUT_PATH` succeeds.
    - `generate_table5(botsim_path, reddit_path, twibot_path, output_path)` writes a LaTeX file at `output_path`, returns the `output_path` string.
    - The returned LaTeX file contains `BotSim-24` (column header).
    - Calling `generate_table5` with a non-existent metrics path raises `FileNotFoundError`.
  </behavior>
  <action>
    Create `generate_table5.py`:

        from __future__ import annotations

        import json
        import os
        import sys

        from ablation_tables import generate_cross_dataset_table, save_latex

        DEFAULT_BOTSIM_METRICS_PATH = os.path.join("paper_outputs", "metrics_botsim.json")
        DEFAULT_REDDIT_TRANSFER_METRICS_PATH = os.path.join("paper_outputs", "metrics_reddit_transfer.json")
        DEFAULT_TWIBOT_NATIVE_METRICS_PATH = os.path.join("paper_outputs", "metrics_twibot_native.json")
        DEFAULT_TABLE5_OUTPUT_PATH = os.path.join("tables", "table5_cross_dataset.tex")


        def _load_metrics(path: str) -> dict:
            # raises FileNotFoundError if path does not exist — caller expects this.
            with open(path, encoding="utf-8") as f:
                return json.load(f)


        def generate_table5(
            botsim_metrics_path: str = DEFAULT_BOTSIM_METRICS_PATH,
            reddit_transfer_metrics_path: str = DEFAULT_REDDIT_TRANSFER_METRICS_PATH,
            twibot_native_metrics_path: str = DEFAULT_TWIBOT_NATIVE_METRICS_PATH,
            output_path: str = DEFAULT_TABLE5_OUTPUT_PATH,
        ) -> str:
            botsim_metrics = _load_metrics(botsim_metrics_path)
            reddit_transfer_metrics = _load_metrics(reddit_transfer_metrics_path)
            twibot_native_metrics = _load_metrics(twibot_native_metrics_path)

            df_t5 = generate_cross_dataset_table(
                botsim_metrics,
                reddit_transfer_metrics,
                twibot_native_metrics,
            )
            save_latex(df_t5, output_path)
            print(f"[table5] Saved LaTeX to {output_path}")
            return output_path


        if __name__ == "__main__":
            botsim_path = sys.argv[1] if len(sys.argv) > 1 else DEFAULT_BOTSIM_METRICS_PATH
            reddit_path = sys.argv[2] if len(sys.argv) > 2 else DEFAULT_REDDIT_TRANSFER_METRICS_PATH
            twibot_path = sys.argv[3] if len(sys.argv) > 3 else DEFAULT_TWIBOT_NATIVE_METRICS_PATH
            out_path    = sys.argv[4] if len(sys.argv) > 4 else DEFAULT_TABLE5_OUTPUT_PATH
            generate_table5(botsim_path, reddit_path, twibot_path, out_path)

    Do NOT modify `ablation_tables.py`. The broken `from main import filter_edges_for_split` is fixed in Plan 20-01 Task 1; no further change is needed here.

    Do NOT re-implement `generate_cross_dataset_table` or `save_latex` — both already exist and are tested.

    Do NOT call `ablation_tables.main()` — that function reads stale Phase 15/16 defaults and computes Tables 1-4, which is out of scope for Phase 20.

    Commit message: `feat(20): add generate_table5 driver reading paper_outputs JSONs`
  </action>
  <verify>
    <automated>python -m py_compile generate_table5.py &amp;&amp; python -m pytest tests/test_paper_outputs.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `generate_table5.py` exists and `python -m py_compile generate_table5.py` exits 0.
    - `python -m pytest tests/test_paper_outputs.py -x -q` exits 0 (all Wave 0 tests pass).
    - `grep "from ablation_tables import generate_cross_dataset_table, save_latex" generate_table5.py` returns a match.
    - `grep "paper_outputs" generate_table5.py` returns at least three matches (one per default metrics path).
    - `grep "table5_cross_dataset.tex" generate_table5.py` returns a match.
    - `grep 'if __name__ == "__main__"' generate_table5.py` returns a match.
    - `grep "ablation_tables.main" generate_table5.py` returns NO matches (anti-regression — must not call the stale Tables 1-4 main block).
    - Smoke run with synthetic inputs:
      `python -c "import json, tempfile, os; from generate_table5 import generate_table5; m={'overall':{'f1':0.5,'auc':0.5,'precision':0.5,'recall':0.5},'per_stage':{'p1':{'f1':0.5,'auc':0.5,'precision':0.5,'recall':0.5},'p2':{'f1':0.5,'auc':0.5,'precision':0.5,'recall':0.5},'p12':{'f1':0.5,'auc':0.5,'precision':0.5,'recall':0.5},'p_final':{'f1':0.5,'auc':0.5,'precision':0.5,'recall':0.5}},'routing':{'pct_stage1_exit':0.0,'pct_stage2_exit':0.0,'pct_stage3_exit':0.0,'pct_amr_triggered':0.0}}; d=tempfile.mkdtemp(); ps=[os.path.join(d,n) for n in ('b.json','r.json','t.json')]; [open(p,'w').write(json.dumps(m)) for p in ps]; o=os.path.join(d,'t5.tex'); generate_table5(*ps, o); assert os.path.exists(o); assert 'BotSim-24' in open(o).read(); print('OK')"` exits 0.
  </acceptance_criteria>
  <done>`generate_table5.py` reads three `paper_outputs/` JSONs, writes `tables/table5_cross_dataset.tex` with the correct column headers, and turns all Wave 0 Table 5 tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Phase-level verification — full eval + Table 5 suite is green</name>
  <files>.planning/phases/20-evaluation-entry-points-and-paper-outputs/20-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/20-evaluation-entry-points-and-paper-outputs/20-VALIDATION.md (the per-task verification map)
    - .planning/phases/20-evaluation-entry-points-and-paper-outputs/20-01-SUMMARY.md through 20-04-SUMMARY.md
  </read_first>
  <action>
    Run the full Phase 20 test surface and record results in `20-VERIFICATION.md`. Required commands, executed in order:

    1. `python -m pytest tests/test_eval_botsim_native.py tests/test_eval_reddit_twibot_transfer.py tests/test_eval_twibot_native.py tests/test_paper_outputs.py -x -q`
    2. `python -m pytest tests/test_ablation_tables.py --collect-only -q` (collection must succeed)
    3. `python -m pytest tests/test_evaluate.py tests/test_evaluate_twibot20_native.py -x -q` (pre-existing green tests must remain green)
    4. End-to-end paper-output smoke run if `trained_system_botsim.joblib` and `trained_system_twibot.joblib` are present AND TwiBot `test.json` + BotSim data files are present:
       a. `python eval_botsim_native.py`
       b. `python eval_reddit_twibot_transfer.py`
       c. `python eval_twibot_native.py`
       d. `python generate_table5.py`
       e. Confirm all six files exist: `paper_outputs/metrics_botsim.json`, `paper_outputs/confusion_matrix_botsim.png`, `paper_outputs/metrics_reddit_transfer.json`, `paper_outputs/confusion_matrix_reddit_transfer.png`, `paper_outputs/metrics_twibot_native.json`, `paper_outputs/confusion_matrix_twibot_native.png`, plus `tables/table5_cross_dataset.tex`.

    Record in `20-VERIFICATION.md`:
    - Per-requirement table (EVAL-01, EVAL-02, EVAL-03, PAPER-01, PAPER-02, PAPER-03): test command, pass/fail, evidence path.
    - Environment blockers: if any model artifact or data file is missing locally, note it explicitly. Do NOT attempt to fabricate evidence.
    - Whether Step 4 (end-to-end) ran fully, partially (some scripts), or was blocked (missing artifacts).

    Commit message: `docs(20): phase 20 verification report`
  </action>
  <verify>
    <automated>python -m pytest tests/test_eval_botsim_native.py tests/test_eval_reddit_twibot_transfer.py tests/test_eval_twibot_native.py tests/test_paper_outputs.py -x -q &amp;&amp; python -m pytest tests/test_evaluate.py tests/test_evaluate_twibot20_native.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `20-VERIFICATION.md` exists under `.planning/phases/20-evaluation-entry-points-and-paper-outputs/`.
    - The report includes one row per requirement ID (EVAL-01, EVAL-02, EVAL-03, PAPER-01, PAPER-02, PAPER-03) with the test command run and the pass/fail outcome.
    - All four new-in-Phase-20 test files pass: `python -m pytest tests/test_eval_botsim_native.py tests/test_eval_reddit_twibot_transfer.py tests/test_eval_twibot_native.py tests/test_paper_outputs.py -x -q` exits 0.
    - Pre-existing tests still green: `python -m pytest tests/test_evaluate.py tests/test_evaluate_twibot20_native.py -x -q` exits 0.
    - `tests/test_ablation_tables.py --collect-only -q` exits 0 (the Plan 20-01 import fix held).
    - If end-to-end Step 4 was blocked by missing artifacts/data, the blocker is recorded clearly in `20-VERIFICATION.md` (note: Phase 20 is free to ship the code surface even if Phase 19 artifacts are not yet present locally; unit tests are the product contract).
  </acceptance_criteria>
  <done>All Phase 20 automated tests are green, ablation_tables collection is unblocked, and the verification report documents the exact environment state at phase exit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Local filesystem only: reads three JSON files from `paper_outputs/`, writes one `.tex` file under `tables/`. No network I/O, no auth, no user input beyond filesystem paths via `sys.argv`.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-06 | N/A | local file read/write only | accept | ASVS L1 categories do not apply — no network, no authenticated inputs, no user-facing interface. |
| T-20-07 | Integrity | Table 5 driver reads stale Phase 15/16 artifact paths (RESEARCH.md Pitfall 4) | mitigate | `generate_table5.py` uses its own `paper_outputs/` defaults and never reads `ablation_tables.DEFAULT_NATIVE_METRICS_PATH` or `DEFAULT_REDDIT_TRANSFER_METRICS_PATH`. Acceptance grep confirms paths point to `paper_outputs/`, and the anti-regression assertion in `tests/test_paper_outputs.py::test_generate_table5_defaults_point_to_paper_outputs` catches drift. |
</threat_model>

<verification>
- `python -m pytest tests/test_paper_outputs.py -x -q` exits 0
- `python -m py_compile generate_table5.py` exits 0
- Full Phase 20 test suite green: `python -m pytest tests/test_eval_botsim_native.py tests/test_eval_reddit_twibot_transfer.py tests/test_eval_twibot_native.py tests/test_paper_outputs.py -x -q` exits 0
- `20-VERIFICATION.md` records all six requirement outcomes with evidence
</verification>

<success_criteria>
- `generate_table5.py` is callable from `python generate_table5.py` and produces `tables/table5_cross_dataset.tex` without manual steps.
- The LaTeX output has the four Table 5 rows (F1, AUC-ROC, Precision, Recall) and the three cross-dataset columns.
- Default input paths point exclusively to `paper_outputs/` files (Phase 20 outputs), never to Phase 15/16 legacy directories.
- Phase 20 exits with every new test file green and the verification report recording the exact environment state.
</success_criteria>

<output>
After completion, create `.planning/phases/20-evaluation-entry-points-and-paper-outputs/20-05-SUMMARY.md` and `.planning/phases/20-evaluation-entry-points-and-paper-outputs/20-VERIFICATION.md`.
</output>
