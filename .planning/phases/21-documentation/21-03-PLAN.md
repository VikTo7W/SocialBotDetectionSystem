---
phase: 21
plan: 03
type: execute
wave: 2
depends_on:
  - 21-01-PLAN.md
  - 21-02-PLAN.md
files_modified:
  - README.md
  - VERSION.md
autonomous: true
requirements:
  - DOC-03
must_haves:
  truths:
    - "README reproduction commands match the maintained training and evaluation entry points"
    - "Artifact names and output paths match the v1.5 contract"
    - "Deferred TwiBot retraining/runtime caveats are documented honestly"
---

# Plan 21-03

## Objective

Rewrite the reproduction and release-facing docs so `README.md` and `VERSION.md` match the maintained v1.5 scripts, artifact names, output paths, and current verification caveats.

## Tasks

1. Replace outdated reproduction commands with the maintained training and evaluation commands:
   - `train_botsim.py`
   - `train_twibot.py`
   - `eval_botsim_native.py`
   - `eval_reddit_twibot_transfer.py`
   - `eval_twibot_native.py`
   - `generate_table5.py`
2. Update artifact names to the maintained v1.5 contract:
   - `trained_system_botsim.joblib`
   - `trained_system_twibot.joblib`
3. Route expected outputs to `paper_outputs/` and `tables/` instead of older phase artifact folders.
4. Align `VERSION.md` with the new release story and the deduplicated evaluation surface.
5. Document the current TwiBot-native caveat accurately: Phase 20 code is complete, but full local runtime smoke still depends on the deferred `trained_system_twibot.joblib` rebuild.

## Verification

- read back `README.md` and `VERSION.md`
- search for stale references to `trained_system_v12.joblib`, `trained_system_twibot20.joblib`, `evaluate_twibot20.py`, and `evaluate_twibot20_native.py`

## Acceptance Criteria

- `README.md` includes a full reproduction guide with data requirements, commands, and expected outputs for all maintained paths.
- `VERSION.md` is updated to the v1.5 modular release story.
- Output paths point to `paper_outputs/` and `tables/`, not retired phase artifact directories.
- Deferred/runtime caveats are explicit and not misrepresented as completed evidence.
