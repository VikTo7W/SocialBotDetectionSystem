---
phase: 21
plan: 02
type: execute
wave: 1
depends_on:
  - 21-01-PLAN.md
files_modified:
  - README.md
autonomous: true
requirements:
  - DOC-02
must_haves:
  truths:
    - "README documents feature-stage mapping for both BotSim-24 and TwiBot-20"
    - "README reflects the maintained Stage 2b AMR-only contract"
    - "README distinguishes dataset-specific signals without implying fake cross-dataset parity"
---

# Plan 21-02

## Objective

Add a feature-stage reference to `README.md` that shows, for both datasets, which signals feed Stage 1, Stage 2a, Stage 2b, and Stage 3 in the maintained shared extractor architecture.

## Tasks

1. Inspect the shared extractor modules and summarize the maintained Stage 1, Stage 2, and Stage 3 feature families without copying code verbatim.
2. Add a dataset-by-stage mapping section for:
   - BotSim-24
   - TwiBot-20
3. Make clear that Stage 2b is AMR embedding delta-logit only in v1.5 and that the LSTM path is removed.
4. Explain where dataset support diverges because the underlying data differs, especially for transfer evaluation and graph inputs.

## Verification

- read back the new feature-stage section in `README.md`
- sanity-check the stage mapping against `features/stage1.py`, `features/stage2.py`, and `features/stage3.py`

## Acceptance Criteria

- `README.md` contains a reader-friendly feature-stage mapping for both maintained datasets.
- Stage 1, Stage 2a, Stage 2b, and Stage 3 are all covered explicitly.
- The Stage 2b section documents the AMR-only maintained path and does not mention an active LSTM implementation.
- The mapping is faithful to the shared extractor modules rather than the removed legacy scripts.
