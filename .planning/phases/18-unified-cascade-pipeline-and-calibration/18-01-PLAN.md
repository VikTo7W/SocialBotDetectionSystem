---
phase: 18
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_calibrate.py
  - tests/test_train_twibot20.py
  - tests/test_evaluate.py
  - tests/conftest.py
autonomous: true
requirements:
  - CORE-03
  - CORE-04
  - QUAL-01
must_haves:
  truths:
    - "Tests pin a shared pipeline surface rather than legacy monkeypatch-only behavior"
    - "Calibration tests pin the maintained single-trial contract instead of multi-trial plateau behavior"
    - "Refactor safety tests exist before shared pipeline implementation begins"
---

# Plan 18-01

## Objective

Create the red-test safety net for the Phase 18 refactor. Update tests so they assert the desired shared pipeline and single-trial calibration behavior before implementation changes begin.

## Tasks

1. Update calibration tests to match the maintained v1.5 contract.
2. Add or update shared-pipeline tests that pin dataset-aware orchestration and remove dependence on `native_feature_overrides()` as the long-term integration mechanism.
3. Keep compatibility expectations explicit where Phase 18 is allowed to preserve wrappers temporarily.

## Verification

- `python -m py_compile tests/test_calibrate.py tests/test_train_twibot20.py tests/test_evaluate.py tests/conftest.py`
- targeted pytest for the updated planning-surface tests

## Acceptance Criteria

- Tests no longer require multi-trial plateau-stop behavior as the maintained calibration contract.
- Tests define the expected shared pipeline API and dataset-aware call flow.
- Red-state failures, if any, point at the new shared pipeline surface rather than old procedural details.
