---
phase: 18
plan: 03
type: execute
wave: 1
depends_on:
  - 18-01
files_modified:
  - calibrate.py
  - tests/test_calibrate.py
autonomous: true
requirements:
  - CORE-04
  - QUAL-01
must_haves:
  truths:
    - "Maintained calibration executes exactly one trial"
    - "Calibration reporting remains stable enough for downstream artifact writing"
    - "Calibration code is simpler than the retired multi-trial implementation"
---

# Plan 18-03

## Objective

Simplify calibration to the maintained single-trial v1.5 contract.

## Tasks

1. Remove or retire the multi-trial Optuna search loop, plateau stopping, and alternative-trial ranking from the maintained path.
2. Keep a stable calibration report payload for downstream artifact writers and tests.
3. Update tests to assert single-trial execution and reproducible output.

## Verification

- `python -m py_compile calibrate.py tests/test_calibrate.py`
- targeted pytest around calibration report shape and one-trial behavior

## Acceptance Criteria

- Maintained calibration executes one trial only.
- `system.calibration_report_` still provides stable evidence for downstream reporting.
- The code no longer advertises multi-trial behavior as part of the maintained contract.
