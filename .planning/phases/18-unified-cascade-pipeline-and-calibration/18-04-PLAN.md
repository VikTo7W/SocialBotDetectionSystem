---
phase: 18
plan: 04
type: execute
wave: 2
depends_on:
  - 18-02
  - 18-03
files_modified:
  - main.py
  - train_twibot20.py
  - evaluate_twibot20_native.py
  - tests/test_train_twibot20.py
  - .planning/phases/18-unified-cascade-pipeline-and-calibration/18-VERIFICATION.md
autonomous: true
requirements:
  - CORE-03
  - CORE-04
  - QUAL-02
must_haves:
  truths:
    - "Current BotSim and TwiBot training callers use the shared pipeline core"
    - "TwiBot training no longer depends on maintained monkeypatching as the primary orchestration mechanism"
    - "Phase-level verification demonstrates shared pipeline and single-trial calibration integration"
---

# Plan 18-04

## Objective

Migrate the current maintained BotSim and TwiBot callers onto the shared pipeline core, then verify the phase end-to-end.

## Tasks

1. Update `main.py` and `train_twibot20.py` to call the shared pipeline implementation directly.
2. Keep compatibility only where needed for existing entry points and tests.
3. Verify the integrated Phase 18 contract and document any remaining Windows pytest environment gaps.

## Verification

- `python -m py_compile main.py train_twibot20.py evaluate_twibot20_native.py`
- targeted pytest for training-caller integration tests
- phase-level verification report in `18-VERIFICATION.md`

## Acceptance Criteria

- Both maintained training callers consume the same shared pipeline implementation.
- Single-trial calibration is used consistently by the maintained training paths.
- Phase 18 ends ready for Phase 19 entry-point cleanup rather than still blocked on shared-core refactoring.
