---
phase: 19
plan: 04
type: execute
wave: 2
depends_on:
  - 19-02
  - 19-03
files_modified:
  - trained_system_botsim.joblib
  - trained_system_twibot.joblib
  - .planning/phases/19-training-entry-points-and-fresh-model-retraining/19-VERIFICATION.md
autonomous: true
requirements:
  - TRAIN-01
  - TRAIN-02
must_haves:
  truths:
    - "Fresh BotSim and TwiBot artifacts are produced by the new maintained entry points"
    - "Both new artifacts load without error after training"
    - "Phase-level verification records any environment limits separately from product-code readiness"
---

# Plan 19-04

## Objective

Run the new maintained training entry points, generate fresh artifacts, and verify Phase 19 end-to-end.

## Tasks

1. Execute `train_botsim.py` to build `trained_system_botsim.joblib` from the unified pipeline code.
2. Execute `train_twibot.py` to build `trained_system_twibot.joblib` from the unified pipeline code.
3. Smoke-load both artifacts and verify the maintained scripts report stable output paths and seeded reproducibility inputs.
4. Record the final verification evidence, including any runtime or local-data blockers that affect retraining.

## Verification

- `python train_botsim.py`
- `python train_twibot.py`
- direct smoke load of both fresh joblib artifacts
- phase-level verification report in `19-VERIFICATION.md`

## Acceptance Criteria

- Both maintained training scripts complete without overwriting the other dataset's artifact.
- `trained_system_botsim.joblib` and `trained_system_twibot.joblib` both load without error.
- Phase 19 ends ready for Phase 20 evaluation-surface cleanup rather than still blocked on training-entry-point migration.
