---
phase: 19
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_train_botsim.py
  - tests/test_train_twibot.py
  - tests/test_train_twibot20.py
  - tests/test_evaluate_twibot20_native.py
autonomous: true
requirements:
  - TRAIN-01
  - TRAIN-02
must_haves:
  truths:
    - "Tests pin `train_botsim.py` and `train_twibot.py` as the maintained training entry points"
    - "The new default artifact names are protected from cross-dataset overwrite"
    - "Phase 19 starts from explicit contract tests rather than ad hoc script renaming"
---

# Plan 19-01

## Objective

Create the red-test safety net for the maintained v1.5 training surface before renaming or extracting entry points.

## Tasks

1. Add focused tests for a new `train_botsim.py` entry point, including default artifact path, artifact isolation, and summary payload shape.
2. Add focused tests for a new `train_twibot.py` entry point and update TwiBot training expectations away from the older `train_twibot20.py` maintained contract.
3. Keep compatibility expectations explicit where the old script names may remain as temporary shims during migration.

## Verification

- `python -m py_compile tests/test_train_botsim.py tests/test_train_twibot.py tests/test_train_twibot20.py tests/test_evaluate_twibot20_native.py`
- targeted pytest for the new training entry-point contract tests

## Acceptance Criteria

- The maintained training commands are named and tested before implementation changes land.
- Tests assert the new artifact defaults `trained_system_botsim.joblib` and `trained_system_twibot.joblib`.
- Any compatibility with older script names is clearly marked as temporary rather than the maintained contract.
