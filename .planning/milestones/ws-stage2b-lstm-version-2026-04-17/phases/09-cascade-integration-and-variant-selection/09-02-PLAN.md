---
phase: 09-cascade-integration-and-variant-selection
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - botdetector_pipeline.py
  - tests/conftest.py
  - tests/test_calibrate.py
autonomous: true
requirements:
  - LSTM-04
  - LSTM-05
  - LSTM-06

must_haves:
  truths:
    - "Phase 9 proof should come from deterministic integration tests rather than an early benchmark"
    - "predict_system() must honor the same explicit Stage 2b variant choice established during training"
    - "The shared cascade pipeline should differ only at the Stage 2b branch when AMR and LSTM variants are compared"
  artifacts:
    - path: "botdetector_pipeline.py"
      provides: "Inference-time Stage 2b variant routing in the shared cascade"
      contains: "predict_system() behavior for both AMR and LSTM variants"
    - path: "tests/conftest.py"
      provides: "Fixture support for end-to-end Stage 2b integration tests"
      contains: "repeatable setup for AMR and LSTM variant selection checks"
    - path: "tests/test_calibrate.py"
      provides: "Deterministic integration coverage for Stage 2b variant selection"
      contains: "training-plus-inference assertions over AMR vs LSTM branch routing"
  key_links:
    - from: "botdetector_pipeline.py"
      to: "tests/conftest.py"
      via: "shared fixture assembly for both Stage 2b variants"
      pattern: "train_system"
    - from: "botdetector_pipeline.py"
      to: "tests/test_calibrate.py"
      via: "end-to-end variant selection and prediction contract"
      pattern: "predict_system"
---

<objective>
Finish Phase 9 by wiring explicit Stage 2b selection through inference and adding deterministic integration tests that prove the cascade can train and predict with either AMR or LSTM while keeping the rest of the pipeline shared.

Purpose: A training-time selector alone is not enough. This plan completes the end-to-end contract and leaves behind repeatable proof that the new LSTM path is a real cascade option rather than a disconnected prototype.
Output: End-to-end Stage 2b variant routing plus deterministic integration-test coverage.
</objective>

<execution_context>
@.planning/workstreams/stage2b-lstm-version/phases/09-cascade-integration-and-variant-selection/09-CONTEXT.md
@.planning/workstreams/stage2b-lstm-version/phases/09-cascade-integration-and-variant-selection/09-01-PLAN.md
@botdetector_pipeline.py
@tests/conftest.py
@tests/test_calibrate.py
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Wire explicit Stage 2b variant selection through prediction</name>
  <files>botdetector_pipeline.py</files>
  <read_first>
    - botdetector_pipeline.py after Plan 01
    - current predict_system() logic
    - current Stage2LSTMRefiner and AMRDeltaRefiner contracts
  </read_first>
  <behavior>
    - Use the same explicit variant semantics at inference time that training established.
    - Keep downstream outputs identical in shape and meaning regardless of which Stage 2b branch is active.
    - Fail clearly if the configured branch is unavailable rather than silently falling back.
  </behavior>
  <action>
Complete end-to-end Stage 2b variant routing in the cascade.

Specifically:
1. Update `predict_system()` so it applies the selected Stage 2b branch intentionally.
2. Preserve the existing AMR behavior when `amr` is selected.
3. Route through the LSTM refiner when `lstm` is selected, reusing the deterministic sequence contract from Phase 8.
4. Keep the rest of the cascade path shared and contract-compatible.
  </action>
  <verify>
    <automated>python -m pytest tests/test_calibrate.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `predict_system()` honors explicit Stage 2b variant selection
    - AMR and LSTM both produce the same high-level output contract
    - The implementation avoids silent implicit fallback between variants
  </acceptance_criteria>
  <done>The cascade can intentionally predict with either Stage 2b branch while preserving the same surrounding contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add deterministic integration tests for Stage 2b variant selection</name>
  <files>tests/conftest.py, tests/test_calibrate.py</files>
  <read_first>
    - tests/conftest.py
    - tests/test_calibrate.py
    - botdetector_pipeline.py after Task 1
  </read_first>
  <behavior>
    - Prefer compact end-to-end contract checks over brittle implementation-detail assertions.
    - Prove both variant selection visibility and shared-pipeline behavior under deterministic fixtures.
    - Keep the proof scoped to integration correctness rather than comparative quality.
  </behavior>
  <action>
Add regression coverage that proves the new Stage 2b integration contract.

Specifically:
1. Extend fixtures as needed so tests can build systems for both `amr` and `lstm` Stage 2b variants.
2. Add integration tests that verify:
   - training records the intended variant explicitly
   - prediction uses the configured branch
   - both variants preserve the same output-level contract
   - the AMR baseline path still works after LSTM integration
3. Keep these tests deterministic and fixture-backed rather than introducing a benchmark harness.
  </action>
  <verify>
    <automated>python -m pytest tests/test_calibrate.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Tests cover both AMR and LSTM Stage 2b selection paths
    - The integration contract is protected against accidental silent fallback or branch drift
    - The proof remains deterministic and Phase 9-scoped
  </acceptance_criteria>
  <done>Phase 9 has repeatable automated proof that Stage 2b variant selection works end to end.</done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/test_calibrate.py -x -q` - end-to-end Stage 2b selection tests pass
2. The cascade can train and predict with either explicit Stage 2b variant
3. The AMR baseline path remains available and behaviorally intact as the control branch
</verification>

<success_criteria>
- The LSTM Stage 2b path is integrated through both training and prediction
- Variant choice is explicit, visible, and tested
- Requirements `LSTM-04`, `LSTM-05`, and `LSTM-06` have an executable implementation path
</success_criteria>
