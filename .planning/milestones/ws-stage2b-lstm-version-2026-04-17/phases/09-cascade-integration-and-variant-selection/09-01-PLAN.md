---
phase: 09-cascade-integration-and-variant-selection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - botdetector_pipeline.py
autonomous: true
requirements:
  - LSTM-04
  - LSTM-05

must_haves:
  truths:
    - "Phase 9 must use an explicit config-controlled Stage 2b variant switch rather than implicit object detection"
    - "The AMR delta-refiner path must remain available as the baseline implementation"
    - "Everything outside the Stage 2b branch should stay as shared as practical so AMR vs LSTM remains an apples-to-apples comparison"
  artifacts:
    - path: "botdetector_pipeline.py"
      provides: "Explicit Stage 2b variant selection and training-time integration"
      contains: "shared cascade training flow that can populate either the AMR or LSTM Stage 2b branch"
  key_links:
    - from: "botdetector_pipeline.py"
      to: "botdetector_pipeline.py"
      via: "train_system() populating TrainedSystem with an intentional Stage 2b variant"
      pattern: "TrainedSystem"
---

<objective>
Introduce a clear Stage 2b variant selector into the training flow so the cascade can intentionally build either the AMR baseline or the LSTM variant without changing the rest of the pipeline contract.

Purpose: Phase 8 proved the LSTM foundation in isolation. Phase 9 starts by making variant choice explicit at training time so later inference and evaluation work operate on a real end-to-end system state instead of ad hoc hooks.
Output: A shared training pipeline with an explicit AMR-vs-LSTM Stage 2b selector recorded in `TrainedSystem`.
</objective>

<execution_context>
@.planning/workstreams/stage2b-lstm-version/phases/09-cascade-integration-and-variant-selection/09-CONTEXT.md
@.planning/workstreams/stage2b-lstm-version/REQUIREMENTS.md
@botdetector_pipeline.py
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Add an explicit Stage 2b variant selector to the shared training path</name>
  <files>botdetector_pipeline.py</files>
  <read_first>
    - botdetector_pipeline.py
    - 09-CONTEXT.md decisions D-01 through D-06
    - current train_system() and TrainedSystem definitions
  </read_first>
  <behavior>
    - Keep variant choice explicit in function inputs and stored model state.
    - Preserve the current AMR behavior as the default or baseline path unless a different explicit choice is made.
    - Avoid broad pipeline rewrites that would dilute future AMR-vs-LSTM comparison value.
  </behavior>
  <action>
Integrate Stage 2b variant selection into the training contract.

Specifically:
1. Introduce a clear config or argument surface that selects `amr` vs `lstm` for Stage 2b.
2. Teach `train_system()` to train the selected Stage 2b branch while keeping the rest of the cascade flow shared.
3. Ensure `TrainedSystem` captures enough explicit state that downstream inference can tell which Stage 2b branch was intended.
4. Preserve the AMR branch semantics and baseline availability.
  </action>
  <verify>
    <automated>python -m pytest tests/test_calibrate.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `train_system()` can intentionally select a Stage 2b variant
    - `TrainedSystem` records the active Stage 2b variant explicitly
    - The AMR baseline path still trains successfully in the shared flow
    - The LSTM path is integrated without changing unrelated cascade semantics
  </acceptance_criteria>
  <done>The training contract can build either Stage 2b branch intentionally and preserve shared-pipeline semantics.</done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/test_calibrate.py -x -q` - existing fixture-backed Stage 2b contract checks still pass after training-flow integration
2. The code exposes an explicit Stage 2b selector and stores the chosen variant in trained system state
3. The AMR and LSTM branches share the same surrounding cascade pipeline
</verification>

<success_criteria>
- Phase 9 has an explicit Stage 2b training selector instead of implicit branch choice
- The AMR baseline remains intact while the LSTM branch becomes a first-class training option
- Requirements `LSTM-04` and `LSTM-05` have a concrete execution path
</success_criteria>
