---
phase: 08-lstm-stage-2b-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - botdetector_pipeline.py
  - tests/conftest.py
  - tests/test_features_stage2.py
autonomous: true
requirements:
  - LSTM-01
  - LSTM-03

must_haves:
  truths:
    - "Phase 8 establishes the LSTM Stage 2b path as a parallel variant, not a replacement for the AMR refiner"
    - "Sequence input must start from ordered per-message embeddings rather than raw text recurrence"
    - "Deterministic behavior and empty-history handling must be proven before full cascade integration"
  artifacts:
    - path: "botdetector_pipeline.py"
      provides: "LSTM Stage 2b foundation components and sequence preprocessing contract"
      contains: "embedded-message sequence handling with deterministic empty-history behavior"
    - path: "tests/conftest.py"
      provides: "Fixture support for seeded Stage 2b LSTM proofs"
      contains: "minimal train/infer scaffolding for the LSTM variant"
    - path: "tests/test_features_stage2.py"
      provides: "Regression coverage for sequence preprocessing invariants"
      contains: "ordering, empty-history, and deterministic sequence-shaping assertions"
  key_links:
    - from: "botdetector_pipeline.py"
      to: "tests/conftest.py"
      via: "Stage 2b fixture assembly and train/infer scaffolding"
      pattern: "AMRDeltaRefiner"
    - from: "botdetector_pipeline.py"
      to: "tests/test_features_stage2.py"
      via: "message-sequence preprocessing invariants"
      pattern: "extract_amr_embeddings_for_accounts"
---

<objective>
Define the LSTM Stage 2b data/model foundation so the project has a deterministic, testable sequence-processing path that mirrors the current Stage 2b role closely enough for later integration.

Purpose: Phase 8 starts by locking down the sequence contract and preprocessing rules. Before the cascade can select between AMR and LSTM variants, we need a stable definition of what the LSTM consumes, how it handles edge cases, and how reproducibility is enforced.
Output: A seeded LSTM Stage 2b foundation with deterministic sequence preprocessing and fixture-backed proof.
</objective>

<execution_context>
@.planning/workstreams/stage2b-lstm-version/phases/08-lstm-stage-2b-foundation/08-CONTEXT.md
@.planning/workstreams/stage2b-lstm-version/REQUIREMENTS.md
@botdetector_pipeline.py
@tests/conftest.py
@tests/test_features_stage2.py
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Add deterministic embedded-message sequence preparation for the LSTM Stage 2b path</name>
  <files>botdetector_pipeline.py</files>
  <read_first>
    - botdetector_pipeline.py
    - 08-CONTEXT.md decisions D-03, D-04, D-07, D-08
    - tests/test_features_stage2.py for existing empty-message expectations
  </read_first>
  <behavior>
    - Reuse existing message ordering conventions instead of inventing a separate sequence timeline.
    - Define an explicit empty-history rule so the LSTM path is deterministic on zero-message accounts.
    - Keep the AMR baseline path intact while introducing new LSTM-specific helpers.
  </behavior>
  <action>
Add the core sequence-preparation helpers needed by the LSTM variant.

Specifically:
1. Introduce a helper or helpers that convert each account's ordered messages into a sequence of per-message embeddings suitable for an LSTM path.
2. Define deterministic handling for:
   - zero-message accounts
   - short histories
   - truncation/padding behavior
3. Keep this foundation additive: do not delete or semantically repurpose the current AMR embedding path.
4. Make the sequence-preparation contract clear enough that later plans can train and call an LSTM Stage 2b model without guessing tensor shape rules.
  </action>
  <verify>
    <automated>python -m pytest tests/test_features_stage2.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Ordered per-message embedding sequences can be prepared deterministically
    - Zero-message and short-history cases have explicit, stable handling
    - The current AMR path remains available as baseline code
    - The sequence contract is concrete enough for downstream planning
  </acceptance_criteria>
  <done>The codebase has a defined, reproducible sequence input contract for the LSTM Stage 2b variant.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add fixture and invariant coverage for the LSTM Stage 2b foundation</name>
  <files>tests/conftest.py, tests/test_features_stage2.py</files>
  <read_first>
    - tests/conftest.py
    - tests/test_features_stage2.py
    - botdetector_pipeline.py after Task 1
  </read_first>
  <behavior>
    - Build on the current synthetic test style already used for Stage 2 and calibration work.
    - Protect sequence ordering and empty-history rules with explicit assertions.
  </behavior>
  <action>
Extend the test harness so Phase 8 can prove the new foundation deterministically.

Specifically:
1. Add fixture support needed to exercise the LSTM Stage 2b sequence-preparation path under a fixed seed.
2. Add tests that verify:
   - sequence ordering is stable
   - zero-message handling is deterministic
   - short-history handling does not produce shape instability
3. Prefer contract-level assertions rather than brittle implementation-detail tests.
  </action>
  <verify>
    <automated>python -m pytest tests/test_features_stage2.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Tests explicitly cover sequence-preparation invariants for the LSTM path
    - The fixture setup can exercise the new path under a fixed seed
    - Future refactors cannot silently break empty-history or ordering behavior
  </acceptance_criteria>
  <done>Phase 8 has automated proof that the LSTM sequence foundation is deterministic and stable.</done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/test_features_stage2.py -x -q` - sequence foundation invariants pass
2. The code exposes a clear embedded-message sequence contract for Stage 2b LSTM work
3. Empty-history handling remains deterministic and explicit
</verification>

<success_criteria>
- The LSTM Stage 2b path has a concrete sequence-preparation foundation
- Deterministic edge-case handling is codified before integration
- Requirements `LSTM-01` and `LSTM-03` have an executable implementation path
</success_criteria>
