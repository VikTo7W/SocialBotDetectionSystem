---
phase: 09-validation-and-selection-evidence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - calibrate.py
  - main.py
  - tests/test_calibrate.py
  - .planning/workstreams/calibration-fix/phases/09-validation-and-selection-evidence/
autonomous: true
requirements:
  - CALFIX-05
  - CALFIX-06
  - CALFIX-07

must_haves:
  truths:
    - "Phase 9 evidence must come from the real S2 calibration path, not from synthetic-only fixtures"
    - "Meaningful differentiation requires both score separation and behavioral separation between the selected trial and strong alternatives"
    - "This phase must leave behind a compact reproducible artifact that explains the final selection outcome clearly"
  artifacts:
    - path: "calibrate.py"
      provides: "Reusable real-run calibration report data and compact summary helpers"
      contains: "selected trial, near-best alternatives, tie counts, and selection rationale"
    - path: "main.py"
      provides: "Reproducible real-path report emission hook"
      contains: "Phase 9 validation artifact output for S2 calibration runs"
    - path: ".planning/workstreams/calibration-fix/phases/09-validation-and-selection-evidence/"
      provides: "Phase-local evidence artifact"
      contains: "compact real-run summary of selection behavior"
    - path: "tests/test_calibrate.py"
      provides: "Regression coverage for report structure and differentiating-selection behavior"
      contains: "assertions on emitted evidence, not only raw ranking internals"
  key_links:
    - from: "main.py"
      to: "calibrate.py"
      via: "real calibration invocation on S2"
      pattern: "calibrate_thresholds"
    - from: "calibrate.py"
      to: ".planning/workstreams/calibration-fix/phases/09-validation-and-selection-evidence/"
      via: "compact artifact or serializable summary content"
      pattern: "calibration_report_"
    - from: "tests/test_calibrate.py"
      to: "calibrate.py"
      via: "report structure and trial-differentiation regression coverage"
      pattern: "calibration_report_"
---

<objective>
Run the real calibration path in a reproducible way, surface a compact evidence artifact from that run, and prove that the selected threshold policy is justified against nearby alternatives by both smooth-score and behavior-facing evidence.

Purpose: Phase 8 fixed the search mechanics. Phase 9 has to prove that the fix matters on the actual S2 path and that the final winner is meaningfully chosen rather than merely different.
Output: A real-run validation artifact plus code/test support that makes the selection evidence inspectable and repeatable.
</objective>

<execution_context>
@.planning/workstreams/calibration-fix/phases/09-validation-and-selection-evidence/09-CONTEXT.md
@.planning/workstreams/calibration-fix/phases/08-calibration-signal-recovery/08-VERIFICATION.md
@.planning/workstreams/calibration-fix/REQUIREMENTS.md
@calibrate.py
@main.py
@tests/test_calibrate.py
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Promote calibration_report_ into a compact real-run evidence artifact</name>
  <files>calibrate.py, main.py</files>
  <read_first>
    - calibrate.py (existing trial diagnostics and selection report)
    - main.py (real S2 calibration call-site)
    - 09-CONTEXT.md decisions D-01 through D-07
  </read_first>
  <behavior>
    - Reuse the Phase 8 diagnostics instead of inventing a separate reporting path unless the real run proves they are insufficient.
    - Keep the artifact compact, reproducible, and easy to inspect from the workstream directory.
    - Anchor the report to the actual S2 calibration invocation used by the project.
  </behavior>
  <action>
Add a report-emission path for the real calibration run.

Specifically:
1. Extend `calibrate_thresholds()` or a nearby helper so the existing `system.calibration_report_` can produce a compact serializable summary of:
   - selected trial id and thresholds
   - selected primary and secondary scores
   - tie count at the best primary score
   - near-best alternatives and their deltas
   - behavior-facing differences such as routing usage, positive prediction counts, or equivalent real outputs
   - whether early stopping happened and why
2. Update `main.py` so the real S2 calibration path can emit that summary reproducibly into the Phase 9 workstream directory or another deterministic local artifact location.
3. Keep the output compact: the artifact should answer why the chosen trial won without dumping every raw trial field unless that is required for interpretation.
4. Avoid broad recalibration changes in this plan unless the real evidence immediately shows the Phase 8 report is too weak to satisfy the requirement.
  </action>
  <verify>
    <automated>python -m pytest tests/test_calibrate.py -x -q</automated>
    <manual>python main.py</manual>
  </verify>
  <acceptance_criteria>
    - A real calibration run can leave behind a compact evidence artifact
    - The artifact identifies the selected trial and near-best alternatives
    - The artifact explains both score-facing and behavior-facing differences
    - The artifact records whether early stopping occurred and why
  </acceptance_criteria>
  <done>Phase 9 has a reproducible evidence surface for the real calibration path instead of synthetic-only proof.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add regression coverage for emitted evidence and differentiating selection behavior</name>
  <files>tests/test_calibrate.py, calibrate.py</files>
  <read_first>
    - tests/test_calibrate.py
    - calibrate.py
    - 08-VERIFICATION.md (to avoid duplicating Phase 8-only proof)
  </read_first>
  <behavior>
    - Protect the new evidence/report contract from regressions.
    - Focus tests on report contents and deterministic selection justification, not only on implementation details.
  </behavior>
  <action>
Add or extend tests so they verify:
1. The calibration report exposes the selected trial, tie counts, stop reason, and a compact representation of near-best alternatives.
2. The selection evidence includes both smooth-score information and at least one behavior-facing differentiator.
3. The report contract remains deterministic under a fixed seed.
4. If the code stops exposing the information needed for Phase 9 evidence, tests fail even if raw calibration still returns thresholds.
  </action>
  <verify>
    <automated>python -m pytest tests/test_calibrate.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Test coverage asserts on report/evidence structure explicitly
    - Tests enforce both score-facing and behavior-facing comparison fields
    - Deterministic report generation is covered under a fixed seed
    - Existing calibration tests remain green
  </acceptance_criteria>
  <done>The evidence artifact becomes a protected part of the calibration contract.</done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/test_calibrate.py -x -q` - calibration evidence contract is covered
2. `python main.py` - real S2 calibration path emits a compact artifact from an actual run
3. The emitted artifact shows selected trial, near-best alternatives, tie count, and stop reason
</verification>

<success_criteria>
- Phase 9 leaves behind real-run evidence, not only synthetic regression tests
- The selected calibration policy is justified against nearby alternatives by both score and behavior signals
- Requirements `CALFIX-05` through `CALFIX-07` have a concrete implementation path
</success_criteria>
