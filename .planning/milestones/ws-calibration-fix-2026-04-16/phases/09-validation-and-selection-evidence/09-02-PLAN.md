---
phase: 09-validation-and-selection-evidence
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - calibrate.py
  - main.py
  - evaluate.py
  - tests/test_calibrate.py
  - .planning/workstreams/calibration-fix/phases/09-validation-and-selection-evidence/
autonomous: true
requirements:
  - CALFIX-05
  - CALFIX-06
  - CALFIX-07

must_haves:
  truths:
    - "Broad scope is allowed only if the real validation evidence from 09-01 reveals a genuine gap in the Phase 8 fix"
    - "If the real run still cannot distinguish materially different candidates, the response must improve the calibration flow rather than just add nicer reporting"
    - "The final milestone record must state whether the shipped fix is early stopping, revised selection logic, or a hybrid approach and why"
  artifacts:
    - path: "calibrate.py"
      provides: "Any targeted follow-on calibration refinement proven necessary by real validation"
      contains: "selection-policy or report changes driven by Phase 9 evidence"
    - path: "main.py"
      provides: "Final real-run validation path compatible with the chosen Phase 9 outcome"
      contains: "stable artifact output or updated invocation behavior"
    - path: ".planning/workstreams/calibration-fix/phases/09-validation-and-selection-evidence/09-VERIFICATION.md"
      provides: "Milestone-close validation narrative"
      contains: "what the real evidence proved and what final policy shipped"
  key_links:
    - from: "09-01 real-run artifact"
      to: "calibrate.py"
      via: "gap-driven follow-on refinement"
      pattern: "selected trial justification"
    - from: "evaluate.py"
      to: "main.py"
      via: "existing compact reporting patterns if a summary formatter is needed"
      pattern: "report"
---

<objective>
Use the Phase 9 real-run evidence to either finalize the hybrid calibration policy as sufficient or make the minimum necessary calibration-flow refinement so the real path clearly distinguishes meaningful candidates and documents the final policy choice.

Purpose: This wave keeps Phase 9 honest. If validation shows the current fix is enough, execution should stop at validation and documentation. If not, this wave absorbs the targeted follow-up work without pretending the evidence was already good enough.
Output: A final validated calibration policy and milestone record that explains whether the shipped solution is early stopping, revised selection logic, or a hybrid approach.
</objective>

<execution_context>
@.planning/workstreams/calibration-fix/phases/09-validation-and-selection-evidence/09-01-PLAN.md
@.planning/workstreams/calibration-fix/phases/09-validation-and-selection-evidence/09-CONTEXT.md
@calibrate.py
@main.py
@evaluate.py
@tests/test_calibrate.py
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Close any real-run evidence gap revealed by Plan 01</name>
  <files>calibrate.py, main.py, evaluate.py</files>
  <read_first>
    - the emitted artifact or execution notes from 09-01
    - calibrate.py
    - main.py
    - evaluate.py if a compact summary pattern is helpful
  </read_first>
  <behavior>
    - Do not reopen the calibration algorithm without evidence.
    - If a gap exists, make the smallest change that gives the real path meaningful differentiating power or clearer justification.
    - Preserve deterministic seed behavior and Phase 8 guarantees.
  </behavior>
  <action>
Review the real-run artifact from Plan 01 and decide:
1. If the selected trial is already justified by both score and behavior evidence, do not broaden the code unnecessarily. Instead, finalize the report path and milestone documentation.
2. If near-best candidates remain behaviorally indistinguishable or the artifact cannot explain the winner clearly, implement a targeted refinement such as:
   - stronger comparison summarization
   - improved near-best candidate selection for reporting
   - a better secondary/behavioral comparison rule
   - a small calibration-flow adjustment required to expose the real distinction cleanly
3. Keep any follow-on changes tightly tied to the gap surfaced by Plan 01.
  </action>
  <verify>
    <automated>python -m pytest tests/test_calibrate.py -x -q</automated>
    <manual>python main.py</manual>
  </verify>
  <acceptance_criteria>
    - Any additional code change is directly justified by real-run evidence
    - The real calibration path can explain why the final trial won
    - The final behavior remains deterministic and compatible with existing invocation flow
  </acceptance_criteria>
  <done>The real path either validates the existing hybrid fix or receives the minimum evidence-driven refinement needed.</done>
</task>

<task type="auto">
  <name>Task 2: Record the final calibration policy and milestone-close evidence</name>
  <files>.planning/workstreams/calibration-fix/phases/09-validation-and-selection-evidence/, .planning/workstreams/calibration-fix/REQUIREMENTS.md, .planning/workstreams/calibration-fix/ROADMAP.md</files>
  <read_first>
    - 09-CONTEXT.md
    - emitted real-run artifact
    - REQUIREMENTS.md
    - ROADMAP.md
  </read_first>
  <behavior>
    - Convert the execution outcome into explicit milestone evidence.
    - Make it easy to see whether the final fix is early stopping, revised selection logic, or a hybrid approach, and why.
  </behavior>
  <action>
After the real-run evidence is settled:
1. Update the Phase 9 verification and summary artifacts with:
   - what was validated on the real path
   - whether the system differentiated meaningful candidates or stopped early for a documented reason
   - what final policy shipped and why
2. Mark `CALFIX-05`, `CALFIX-06`, and `CALFIX-07` complete only if the evidence actually supports them.
3. Keep the workstream docs honest about any residual uncertainty or caveats.
  </action>
  <verify>
    <manual>Phase 9 summary and verification docs reflect the real-run outcome precisely</manual>
  </verify>
  <acceptance_criteria>
    - Milestone docs explicitly state the final shipped calibration strategy
    - Requirements are only checked off when real evidence supports them
    - Residual risks, if any, are documented rather than hidden
  </acceptance_criteria>
  <done>Phase 9 closes with evidence-backed milestone documentation instead of an implicit conclusion.</done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/test_calibrate.py -x -q` - regression protection still holds
2. `python main.py` - final real-path artifact validates the chosen policy or the evidence-driven refinement
3. Phase 9 docs explicitly state whether the milestone shipped early stopping, revised selection logic, or a hybrid approach
</verification>

<success_criteria>
- Real-run evidence either validates the Phase 8 hybrid fix or directly drives the minimum required refinement
- The milestone records the final calibration policy and why it was chosen
- Phase 9 can close without ambiguity about whether trial count is still redundant on the real path
</success_criteria>
