---
phase: 08-calibration-signal-recovery
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - calibrate.py
  - main.py
  - tests/test_calibrate.py
autonomous: true
requirements:
  - CALFIX-03
  - CALFIX-04

must_haves:
  truths:
    - "The final Phase 8 fix is hybrid: improved candidate ranking first, early stopping only as a plateau guardrail"
    - "When F1 ties, selection must prefer a secondary probability-sensitive score rather than first-trial-wins behavior"
    - "Trial count should no longer be effectively redundant on a flat objective plateau"
  artifacts:
    - path: "calibrate.py"
      provides: "Revised trial ranking and plateau stopping policy"
      contains: "secondary-score tie-break and plateau guardrail"
    - path: "main.py"
      provides: "Updated calibration call-site reporting if needed"
      contains: "phase-appropriate calibration summary"
    - path: "tests/test_calibrate.py"
      provides: "Regression coverage for tie-break logic and early-stop guardrail"
      contains: "selection-rule and plateau-stop tests"
  key_links:
    - from: "main.py"
      to: "calibrate.py"
      via: "top-level calibration invocation"
      pattern: "calibrate_thresholds"
    - from: "tests/test_calibrate.py"
      to: "calibrate.py"
      via: "selection and plateau behavior regression tests"
      pattern: "best trial"
---

<objective>
Implement the hybrid calibration fix: rank tied primary-score trials using a smooth secondary metric and stop early only when diagnostics show the search has entered a stable plateau with no meaningful improvement.

Purpose: This converts the Phase 8 diagnosis into a meaningful selection policy so calibration no longer wastes trials or defaults to arbitrary first-trial winners.
Output: A deterministic ranking/stopping policy plus regression tests showing trial count is no longer effectively redundant.
</objective>

<execution_context>
@.planning/workstreams/calibration-fix/phases/08-calibration-signal-recovery/08-CONTEXT.md
@.planning/workstreams/calibration-fix/phases/08-calibration-signal-recovery/08-01-PLAN.md
@calibrate.py
@main.py
@tests/test_calibrate.py
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Replace first-trial-wins tie handling with deterministic secondary ranking</name>
  <files>calibrate.py</files>
  <read_first>
    - calibrate.py after Plan 01 diagnostics are complete
    - 08-CONTEXT.md decisions D-01 through D-06
  </read_first>
  <behavior>
    - Keep the primary optimization target aligned with Phase 8 decisions.
    - Resolve ties using a probability-sensitive secondary score, not trial order.
    - Preserve deterministic behavior under a fixed seed.
  </behavior>
  <action>
Update `calibrate_thresholds()` so final candidate selection:
1. Keeps the existing primary objective or its diagnosed equivalent as the headline optimization score.
2. Breaks best-score ties with a smooth secondary metric computed from `p_final` probabilities.
3. Reconstructs the chosen `StageThresholds` from the best-ranked candidate under this hybrid rule, not merely from Optuna's first best trial if that would ignore the tie-break policy.
4. Reports the chosen primary score, chosen secondary score, and why the winner beat other tied candidates.
  </action>
  <verify>
    <automated>python -m pytest tests/test_calibrate.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Equal-F1 candidates are rankable by a deterministic secondary score
    - Final threshold selection no longer depends on first-trial-wins behavior when ties exist
    - Returned thresholds still satisfy all search-bound constraints
    - Seed reproducibility remains intact
  </acceptance_criteria>
  <done>The calibration path can pick a meaningful winner among tied headline-score trials.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add plateau guardrail and regression coverage for redundant-trial prevention</name>
  <files>calibrate.py, tests/test_calibrate.py, main.py</files>
  <read_first>
    - calibrate.py
    - tests/test_calibrate.py
    - main.py
  </read_first>
  <behavior>
    - Add an early-stop guardrail only after there is enough evidence that the search is plateaued.
    - Validate that the new policy prevents obviously redundant trial runs while preserving deterministic outcomes.
  </behavior>
  <action>
Implement a plateau-detection rule inside calibration, for example based on a configurable patience window over primary+secondary improvements or unchanged top-candidate diagnostics.

Then:
1. Stop the study early when the search has produced no meaningful improvement for the configured plateau window.
2. Expose whether stopping occurred and why.
3. Add regression tests proving:
   - plateau stopping can trigger on redundant search behavior
   - the selection rule still behaves deterministically
   - trial count is no longer effectively redundant for this path
4. Update `main.py` only if needed to surface the new calibration summary more clearly.
  </action>
  <verify>
    <automated>python -m pytest tests/test_calibrate.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Calibration can stop early on a clearly documented plateau condition
    - The code reports whether early stopping occurred and what triggered it
    - Tests fail if the search silently reverts to redundant fixed-trial plateau behavior
    - `main.py` remains compatible with the updated calibration contract
  </acceptance_criteria>
  <done>The hybrid fix is complete: meaningful tie-breaking first, plateau stopping second, both covered by tests.</done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/test_calibrate.py -x -q` - all calibration tests green
2. A representative calibration run shows secondary-score tie-breaking instead of arbitrary first-best retention
3. A representative plateau scenario can stop early with a documented reason
</verification>

<success_criteria>
- `calibrate_thresholds()` avoids wasting trials on a demonstrably flat plateau
- Selection among tied primary-score trials is meaningful and deterministic
- Phase 8 requirements `CALFIX-03` and `CALFIX-04` are covered by code and tests
</success_criteria>

